# TypingRPG 프로젝트 30일 완성 계획

📅 **계획 기간**: 30일 (10개 Phase)
📂 **프로젝트명**: TyperRPG (타이핑 RPG 게임)
🎯 **목표**: Spring Boot 백엔드 포트폴리오용 경량 타이핑 RPG 게임 완성

---

## 📊 전체 진행률 및 Phase 개요

| Phase | 기간 | 주요 목표 | 완료율 | 상태 |
|-------|------|----------|-------|------|
| **Phase 1** | Day 1-3 | 프론트엔드 마무리 및 배포 | 100% | ✅ 완료 |
| **Phase 2** | Day 4-6 | Spring Boot 기본 설정 | 100% | ✅ 완료 |
| **Phase 3** | Day 7-9 | 데이터베이스 및 엔티티 구현 | 100% | ✅ 완료 |
| **Phase 4** | Day 10-12 | 백엔드 서비스 및 API 구현 | 100% | ✅ 완료 |
| **Phase 5** | Day 13-15 | 프론트엔드-백엔드 연동 | 100% | ✅ 완료 |
| **Phase 6** | Day 16-18 | 백엔드 배포 및 통합 테스트 | 0% | ❌ 대기 |
| **Phase 7** | Day 19-21 | 게임 기능 확장 | 0% | ❌ 대기 |
| **Phase 8** | Day 22-24 | UI/UX 개선 및 리소스 | 0% | ❌ 대기 |
| **Phase 9** | Day 25-27 | 추가 기능 구현 | 0% | ❌ 대기 |
| **Phase 10** | Day 28-30 | 최종 테스트 및 완성 | 0% | ❌ 대기 |

**현재 전체 진행률**: 50% (Phase 1-5 완료)
---

## ✅ 완료된 작업

### 프론트엔드 (95% 완료)
- [x] React + Vite 프로젝트 초기 설정
- [x] 문장 데이터 200개 (5개 카테고리)
- [x] 몬스터 5종, 스테이지 5개, 아이템 8개
- [x] 타이핑 엔진 (WPM, 정확도, CPM 계산)
- [x] 7개 페이지 구현 (Home, ContentSelect, GamePlay, GamePlayBoss, Ranking, Shop, Profile)
- [x] 5개 UI 컴포넌트 (Navbar, GameResult, GameHUD, HealthBar, TypingArea)
- [x] LocalStorage 기반 데이터 저장
- [x] 게임 컨텍스트 및 상태 관리
- [x] 한글 조합 중 오타 체크 방지

---

## 📅 Phase별 상세 계획

---

## 🎯 Phase 1: 프론트엔드 마무리 및 배포 (Day 1-3)

**목표**: 프론트엔드 완성도 100% 달성 및 GitHub Pages 배포

### Day 1: CSS 스타일링 완성
**작업 내용**:
- [ ] 전체 UI 일관성 확인
  - 모든 페이지 색상 테마 통일
  - 버튼 스타일 일관성 (hover, active, disabled 상태)
  - 간격(margin, padding) 통일

- [ ] CSS Variables 적용
  ```css
  :root {
    --color-primary: #d4a574;
    --color-bg-dark: rgba(40, 35, 30, 0.9);
    --color-text-light: #ffffff;
    --color-border: #4a3a35;
    --color-success: #4caf50;
    --color-danger: #f44336;
    --color-warning: #ff9800;
  }
  ```

- [ ] 반응형 제거 (PC 전용)
  - 최소 너비 1024px 고정
  - 모바일 미디어 쿼리 제거

- [ ] 폰트 설정
  - 시스템 폰트 최적화
  - 타이핑 영역용 monospace 폰트 적용

**예상 시간**: 6-8시간
**산출물**: 일관된 UI 스타일, CSS 변수 파일

---

### Day 2: 성능 최적화 및 코드 정리
**작업 내용**:
- [ ] React 성능 최적화
  - `React.memo` 적용 (Navbar, GameHUD, HealthBar, GameResult)
  - `useCallback` 적용 (이벤트 핸들러)
  - `useMemo` 적용 (복잡한 계산, 필터링)

- [ ] 코드 리팩토링
  - 중복 코드 제거
  - 매직 넘버를 상수로 추출
  - 주석 추가 (복잡한 로직)

- [ ] ESLint 경고 수정
  - 모든 ESLint 경고 해결
  - 사용하지 않는 import 제거

- [ ] 빌드 최적화
  - `npm run build` 실행 및 번들 크기 확인
  - 필요 시 코드 스플리팅 적용

**예상 시간**: 6-8시간
**산출물**: 최적화된 React 앱, 클린 코드

---

### Day 3: GitHub Pages 배포 및 최종 테스트
**작업 내용**:
- [ ] Vite 설정 수정
  ```javascript
  // vite.config.js
  export default defineConfig({
    base: '/TyperRPG/',
    plugins: [react()],
  })
  ```

- [ ] 빌드 및 배포
  ```bash
  npm run build
  npm install -g gh-pages
  gh-pages -d dist
  ```

- [ ] GitHub Actions 자동 배포 설정 (선택)
  - `.github/workflows/deploy.yml` 작성

- [ ] 배포 테스트
  - GitHub Pages URL 접속 확인
  - 모든 페이지 동작 확인
  - LocalStorage 저장/로드 확인

- [ ] 최종 테스트 체크리스트
  - [ ] 홈 페이지 → 콘텐츠 선택 → 게임 플레이 → 결과 → 랭킹
  - [ ] 상점 구매 → 프로필에서 스탯 확인
  - [ ] 페이지 새로고침 후 데이터 유지
  - [ ] 여러 게임 플레이 후 레벨업 확인

**예상 시간**: 6-8시간
**산출물**: GitHub Pages 배포된 프론트엔드, Phase 1 완료 보고서

---

## 🔧 Phase 2: Spring Boot 기본 설정 (Day 4-6)

**목표**: Spring Boot 프로젝트 생성 및 기본 구조 설정

### Day 4: Spring Boot 프로젝트 생성
**작업 내용**:
- [ ] Spring Initializr로 프로젝트 생성
  - Project: **Gradle - Groovy**
  - Language: **Java 17**
  - Spring Boot: **3.2.x** (최신 안정 버전)
  - Project Metadata:
    - Group: `com.typingrpg`
    - Artifact: `backend`
    - Name: `TyperRPG-Backend`
    - Package name: `com.typingrpg.backend`
  - Dependencies:
    - Spring Web
    - Spring Data JPA
    - Lombok
    - Validation

- [ ] 프로젝트를 `typing-rpg-backend` 폴더에 생성
  ```bash
  # IntelliJ IDEA에서 프로젝트 열기
  # 또는 VS Code + Java Extension Pack
  ```

- [ ] 기본 폴더 구조 생성
  ```
  src/main/java/com/typingrpg/backend/
  ├── controller/
  ├── service/
  ├── repository/
  ├── entity/
  ├── dto/
  ├── config/
  ├── exception/
  └── BackendApplication.java
  ```

- [ ] `application.yml` 기본 설정
  ```yaml
  server:
    port: 8080

  spring:
    application:
      name: typer-rpg-backend
  ```

**예상 시간**: 3-4시간
**산출물**: Spring Boot 프로젝트 생성, 기본 구조

---

### Day 5: SQLite 설정 및 CORS 설정
**작업 내용**:
- [ ] SQLite JDBC Driver 추가
  ```groovy
  // build.gradle
  dependencies {
      // 기존 dependencies...
      implementation 'org.xerial:sqlite-jdbc:3.45.0.0'
      implementation 'org.hibernate.orm:hibernate-community-dialects:6.4.1'
  }
  ```

- [ ] SQLite 데이터베이스 설정
  ```yaml
  # application.yml
  spring:
    datasource:
      url: jdbc:sqlite:typingrpg.db
      driver-class-name: org.sqlite.JDBC
    jpa:
      database-platform: org.hibernate.community.dialect.SQLiteDialect
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
  ```

- [ ] CORS 설정 (프론트엔드 연동)
  ```java
  @Configuration
  public class WebConfig implements WebMvcConfigurer {
      @Override
      public void addCorsMappings(CorsRegistry registry) {
          registry.addMapping("/api/**")
                  .allowedOrigins(
                      "http://localhost:5173",
                      "https://yourusername.github.io"
                  )
                  .allowedMethods("GET", "POST", "PUT", "DELETE")
                  .allowCredentials(true);
      }
  }
  ```

- [ ] 헬스체크 엔드포인트
  ```java
  @RestController
  @RequestMapping("/api")
  public class HealthController {
      @GetMapping("/health")
      public ResponseEntity<Map<String, String>> health() {
          return ResponseEntity.ok(Map.of("status", "UP"));
      }
  }
  ```

- [ ] 애플리케이션 실행 테스트
  ```bash
  ./gradlew bootRun
  # http://localhost:8080/api/health 접속 확인
  ```

**예상 시간**: 4-5시간
**산출물**: SQLite 연결, CORS 설정, 헬스체크 API

---

### Day 6: 예외 처리 및 공통 응답 구조
**작업 내용**:
- [ ] 공통 응답 DTO
  ```java
  @Data
  @AllArgsConstructor
  public class ApiResponse<T> {
      private boolean success;
      private String message;
      private T data;

      public static <T> ApiResponse<T> success(T data) {
          return new ApiResponse<>(true, "Success", data);
      }

      public static <T> ApiResponse<T> error(String message) {
          return new ApiResponse<>(false, message, null);
      }
  }
  ```

- [ ] 커스텀 예외 클래스
  ```java
  public class PlayerNotFoundException extends RuntimeException {
      public PlayerNotFoundException(String message) {
          super(message);
      }
  }

  public class InsufficientGoldException extends RuntimeException {
      public InsufficientGoldException(String message) {
          super(message);
      }
  }
  ```

- [ ] 글로벌 예외 핸들러
  ```java
  @RestControllerAdvice
  public class GlobalExceptionHandler {

      @ExceptionHandler(PlayerNotFoundException.class)
      public ResponseEntity<ApiResponse<Void>> handlePlayerNotFound(PlayerNotFoundException e) {
          return ResponseEntity.status(HttpStatus.NOT_FOUND)
                  .body(ApiResponse.error(e.getMessage()));
      }

      @ExceptionHandler(InsufficientGoldException.class)
      public ResponseEntity<ApiResponse<Void>> handleInsufficientGold(InsufficientGoldException e) {
          return ResponseEntity.status(HttpStatus.BAD_REQUEST)
                  .body(ApiResponse.error(e.getMessage()));
      }

      @ExceptionHandler(Exception.class)
      public ResponseEntity<ApiResponse<Void>> handleGeneral(Exception e) {
          return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                  .body(ApiResponse.error("서버 오류가 발생했습니다."));
      }
  }
  ```

- [ ] Validation 설정
  ```java
  @Configuration
  public class ValidationConfig {
      @Bean
      public Validator validator() {
          return new LocalValidatorFactoryBean();
      }
  }
  ```

**예상 시간**: 4-5시간
**산출물**: 예외 처리 시스템, 공통 응답 구조

---

## 💾 Phase 3: 데이터베이스 및 엔티티 구현 (Day 7-9)

**목표**: JPA 엔티티 및 Repository 작성

### Day 7: Player 및 GameScore 엔티티
**작업 내용**:
- [ ] **Player 엔티티**
  ```java
  @Entity
  @Table(name = "players")
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  public class Player {
      @Id
      @GeneratedValue(strategy = GenerationType.IDENTITY)
      private Long id;

      @Column(unique = true, nullable = false, length = 50)
      private String nickname;

      @Column(nullable = false)
      private Integer level = 1;

      @Column(nullable = false)
      private Integer exp = 0;

      @Column(nullable = false)
      private Integer gold = 0;

      @Column(nullable = false)
      private Integer hp = 100;

      @Column(nullable = false)
      private Integer maxHp = 100;

      @Column(nullable = false)
      private Integer atk = 10;

      @Column(nullable = false)
      private Integer totalScore = 0;

      @Column(nullable = false)
      private Integer gamesPlayed = 0;

      @Column(nullable = false)
      private Integer gamesWon = 0;

      @CreatedDate
      @Column(nullable = false, updatable = false)
      private LocalDateTime createdAt = LocalDateTime.now();

      @Column
      private LocalDateTime lastLoginAt;
  }
  ```

- [ ] **GameScore 엔티티**
  ```java
  @Entity
  @Table(name = "game_scores")
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  public class GameScore {
      @Id
      @GeneratedValue(strategy = GenerationType.IDENTITY)
      private Long id;

      @ManyToOne(fetch = FetchType.LAZY)
      @JoinColumn(name = "player_id", nullable = false)
      private Player player;

      @Column(nullable = false)
      private Integer contentId;

      @Column(nullable = false)
      private Integer score;

      @Column(nullable = false)
      private Double wpm;

      @Column(nullable = false)
      private Double accuracy;

      @Column(nullable = false)
      private Integer monstersDefeated;

      @Column(nullable = false)
      private Boolean isWin;

      @CreatedDate
      @Column(nullable = false, updatable = false)
      private LocalDateTime playedAt = LocalDateTime.now();
  }
  ```

- [ ] **PlayerRepository**
  ```java
  public interface PlayerRepository extends JpaRepository<Player, Long> {
      Optional<Player> findByNickname(String nickname);
      Boolean existsByNickname(String nickname);
  }
  ```

- [ ] **GameScoreRepository**
  ```java
  public interface GameScoreRepository extends JpaRepository<GameScore, Long> {
      List<GameScore> findTop10ByOrderByScoreDesc();
      List<GameScore> findByPlayerOrderByPlayedAtDesc(Player player);
      List<GameScore> findByPlayerIdOrderByScoreDesc(Long playerId);
  }
  ```

**예상 시간**: 6-8시간
**산출물**: Player, GameScore 엔티티 및 Repository

---

### Day 8: Item 및 PlayerItem 엔티티
**작업 내용**:
- [ ] **Item 엔티티**
  ```java
  @Entity
  @Table(name = "items")
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  public class Item {
      @Id
      @GeneratedValue(strategy = GenerationType.IDENTITY)
      private Long id;

      @Column(nullable = false, length = 100)
      private String name;

      @Column(nullable = false, length = 20)
      private String type; // weapon, armor

      @Column(nullable = false)
      private Integer price;

      @Column
      private Integer atkBonus = 0;

      @Column
      private Integer hpBonus = 0;

      @Column(length = 500)
      private String description;

      @Column(length = 10)
      private String icon;
  }
  ```

- [ ] **PlayerItem 엔티티**
  ```java
  @Entity
  @Table(name = "player_items")
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  public class PlayerItem {
      @Id
      @GeneratedValue(strategy = GenerationType.IDENTITY)
      private Long id;

      @ManyToOne(fetch = FetchType.LAZY)
      @JoinColumn(name = "player_id", nullable = false)
      private Player player;

      @ManyToOne(fetch = FetchType.LAZY)
      @JoinColumn(name = "item_id", nullable = false)
      private Item item;

      @CreatedDate
      @Column(nullable = false, updatable = false)
      private LocalDateTime purchasedAt = LocalDateTime.now();
  }
  ```

- [ ] **ItemRepository**
  ```java
  public interface ItemRepository extends JpaRepository<Item, Long> {
      List<Item> findByType(String type);
      List<Item> findByPriceLessThanEqual(Integer price);
  }
  ```

- [ ] **PlayerItemRepository**
  ```java
  public interface PlayerItemRepository extends JpaRepository<PlayerItem, Long> {
      List<PlayerItem> findByPlayerId(Long playerId);
      Long countByPlayerId(Long playerId);
  }
  ```

**예상 시간**: 6-8시간
**산출물**: Item, PlayerItem 엔티티 및 Repository

---

### Day 9: 초기 데이터 삽입 및 테스트
**작업 내용**:
- [ ] **초기 아이템 데이터 (data.sql)**
  ```sql
  INSERT INTO items (name, type, price, atk_bonus, hp_bonus, description, icon) VALUES
  ('나무 검', 'weapon', 100, 5, 0, '기본적인 나무 검. 공격력이 소폭 증가합니다.', '🗡️'),
  ('철 검', 'weapon', 300, 15, 0, '단단한 철로 만든 검. 공격력이 크게 증가합니다.', '⚔️'),
  ('은빛 대검', 'weapon', 600, 30, 0, '은빛으로 빛나는 대검. 강력한 공격력을 자랑합니다.', '🗡️'),
  ('전설의 검', 'weapon', 1500, 60, 0, '전설로 내려오는 강력한 검. 최상급 공격력.', '⚔️'),
  ('가죽 갑옷', 'armor', 150, 0, 20, '가죽으로 만든 갑옷. 체력이 소폭 증가합니다.', '🛡️'),
  ('철 갑옷', 'armor', 400, 0, 50, '철판으로 만든 튼튼한 갑옷. 체력이 크게 증가합니다.', '🛡️'),
  ('은빛 갑옷', 'armor', 800, 0, 100, '은빛으로 빛나는 중갑옷. 높은 방어력을 제공합니다.', '🛡️'),
  ('드래곤 아머', 'armor', 2000, 0, 200, '드래곤 비늘로 만든 최강의 갑옷. 최상급 방어력.', '🛡️');
  ```

- [ ] 데이터 자동 삽입 설정
  ```yaml
  # application.yml
  spring:
    jpa:
      defer-datasource-initialization: true
    sql:
      init:
        mode: always
        data-locations: classpath:data.sql
  ```

- [ ] Repository 테스트
  ```java
  @SpringBootTest
  public class RepositoryTests {

      @Autowired
      private PlayerRepository playerRepository;

      @Autowired
      private ItemRepository itemRepository;

      @Test
      public void testCreatePlayer() {
          Player player = new Player();
          player.setNickname("TestPlayer");
          playerRepository.save(player);

          Optional<Player> found = playerRepository.findByNickname("TestPlayer");
          assertTrue(found.isPresent());
          assertEquals("TestPlayer", found.get().getNickname());
      }

      @Test
      public void testLoadItems() {
          List<Item> items = itemRepository.findAll();
          assertEquals(8, items.size());
      }
  }
  ```

**예상 시간**: 6-8시간
**산출물**: 초기 데이터, Repository 테스트 완료

---

## 🔌 Phase 4: 백엔드 서비스 및 API 구현 (Day 10-12)

**목표**: 비즈니스 로직 및 REST API 구현

### Day 10: PlayerService 및 PlayerController
**작업 내용**:
- [ ] **PlayerService**
  ```java
  @Service
  @RequiredArgsConstructor
  public class PlayerService {
      private final PlayerRepository playerRepository;

      public Player getOrCreatePlayer(String nickname) {
          return playerRepository.findByNickname(nickname)
                  .orElseGet(() -> {
                      Player newPlayer = new Player();
                      newPlayer.setNickname(nickname);
                      newPlayer.setLastLoginAt(LocalDateTime.now());
                      return playerRepository.save(newPlayer);
                  });
      }

      public Player updatePlayerStats(String nickname, Integer exp, Integer gold) {
          Player player = getOrCreatePlayer(nickname);
          player.setExp(player.getExp() + exp);
          player.setGold(player.getGold() + gold);
          player.setLastLoginAt(LocalDateTime.now());

          // 레벨업 로직
          int requiredExp = player.getLevel() * 100;
          while (player.getExp() >= requiredExp) {
              player.setExp(player.getExp() - requiredExp);
              player.setLevel(player.getLevel() + 1);
              player.setMaxHp(player.getMaxHp() + 10);
              player.setHp(player.getMaxHp());
              player.setAtk(player.getAtk() + 2);
              requiredExp = player.getLevel() * 100;
          }

          return playerRepository.save(player);
      }

      public Player getPlayerById(Long id) {
          return playerRepository.findById(id)
                  .orElseThrow(() -> new PlayerNotFoundException("플레이어를 찾을 수 없습니다."));
      }
  }
  ```

- [ ] **DTO 클래스**
  ```java
  @Data
  public class PlayerDto {
      private Long id;
      private String nickname;
      private Integer level;
      private Integer exp;
      private Integer gold;
      private Integer hp;
      private Integer maxHp;
      private Integer atk;
      private Integer totalScore;
      private Integer gamesPlayed;
      private Integer gamesWon;
  }

  @Data
  public class UpdateStatsRequest {
      @NotNull
      private Integer exp;

      @NotNull
      private Integer gold;
  }
  ```

- [ ] **PlayerController**
  ```java
  @RestController
  @RequestMapping("/api/players")
  @RequiredArgsConstructor
  public class PlayerController {
      private final PlayerService playerService;

      @GetMapping("/{nickname}")
      public ResponseEntity<ApiResponse<PlayerDto>> getPlayer(@PathVariable String nickname) {
          Player player = playerService.getOrCreatePlayer(nickname);
          PlayerDto dto = convertToDto(player);
          return ResponseEntity.ok(ApiResponse.success(dto));
      }

      @PostMapping("/{nickname}/update-stats")
      public ResponseEntity<ApiResponse<PlayerDto>> updateStats(
              @PathVariable String nickname,
              @Valid @RequestBody UpdateStatsRequest request) {
          Player player = playerService.updatePlayerStats(
              nickname, request.getExp(), request.getGold());
          PlayerDto dto = convertToDto(player);
          return ResponseEntity.ok(ApiResponse.success(dto));
      }

      private PlayerDto convertToDto(Player player) {
          PlayerDto dto = new PlayerDto();
          dto.setId(player.getId());
          dto.setNickname(player.getNickname());
          dto.setLevel(player.getLevel());
          dto.setExp(player.getExp());
          dto.setGold(player.getGold());
          dto.setHp(player.getHp());
          dto.setMaxHp(player.getMaxHp());
          dto.setAtk(player.getAtk());
          dto.setTotalScore(player.getTotalScore());
          dto.setGamesPlayed(player.getGamesPlayed());
          dto.setGamesWon(player.getGamesWon());
          return dto;
      }
  }
  ```

**예상 시간**: 6-8시간
**산출물**: PlayerService, PlayerController, DTO

---

### Day 11: GameScoreService 및 GameScoreController
**작업 내용**:
- [ ] **GameScoreService**
  ```java
  @Service
  @RequiredArgsConstructor
  public class GameScoreService {
      private final GameScoreRepository gameScoreRepository;
      private final PlayerService playerService;
      private final PlayerRepository playerRepository;

      @Transactional
      public GameScore saveScore(String nickname, GameScoreDto dto) {
          Player player = playerService.getOrCreatePlayer(nickname);

          GameScore score = new GameScore();
          score.setPlayer(player);
          score.setContentId(dto.getContentId());
          score.setScore(dto.getScore());
          score.setWpm(dto.getWpm());
          score.setAccuracy(dto.getAccuracy());
          score.setMonstersDefeated(dto.getMonstersDefeated());
          score.setIsWin(dto.getIsWin());

          GameScore saved = gameScoreRepository.save(score);

          // 플레이어 통계 업데이트
          player.setGamesPlayed(player.getGamesPlayed() + 1);
          if (dto.getIsWin()) {
              player.setGamesWon(player.getGamesWon() + 1);
          }
          player.setTotalScore(player.getTotalScore() + dto.getScore());
          playerRepository.save(player);

          return saved;
      }

      public List<GameScoreDto> getTop10Rankings() {
          return gameScoreRepository.findTop10ByOrderByScoreDesc()
                  .stream()
                  .map(this::convertToDto)
                  .collect(Collectors.toList());
      }

      public List<GameScoreDto> getPlayerScores(String nickname) {
          Player player = playerService.getOrCreatePlayer(nickname);
          return gameScoreRepository.findByPlayerOrderByPlayedAtDesc(player)
                  .stream()
                  .map(this::convertToDto)
                  .collect(Collectors.toList());
      }

      private GameScoreDto convertToDto(GameScore score) {
          GameScoreDto dto = new GameScoreDto();
          dto.setId(score.getId());
          dto.setNickname(score.getPlayer().getNickname());
          dto.setLevel(score.getPlayer().getLevel());
          dto.setContentId(score.getContentId());
          dto.setScore(score.getScore());
          dto.setWpm(score.getWpm());
          dto.setAccuracy(score.getAccuracy());
          dto.setMonstersDefeated(score.getMonstersDefeated());
          dto.setIsWin(score.getIsWin());
          dto.setPlayedAt(score.getPlayedAt());
          return dto;
      }
  }
  ```

- [ ] **DTO 클래스**
  ```java
  @Data
  public class GameScoreDto {
      private Long id;
      private String nickname;
      private Integer level;

      @NotNull
      private Integer contentId;

      @NotNull
      private Integer score;

      @NotNull
      private Double wpm;

      @NotNull
      private Double accuracy;

      @NotNull
      private Integer monstersDefeated;

      @NotNull
      private Boolean isWin;

      private LocalDateTime playedAt;
  }
  ```

- [ ] **GameScoreController**
  ```java
  @RestController
  @RequestMapping("/api/scores")
  @RequiredArgsConstructor
  public class GameScoreController {
      private final GameScoreService gameScoreService;

      @PostMapping("/{nickname}")
      public ResponseEntity<ApiResponse<GameScoreDto>> saveScore(
              @PathVariable String nickname,
              @Valid @RequestBody GameScoreDto dto) {
          GameScore score = gameScoreService.saveScore(nickname, dto);
          GameScoreDto resultDto = convertToDto(score);
          return ResponseEntity.ok(ApiResponse.success(resultDto));
      }

      @GetMapping("/rankings")
      public ResponseEntity<ApiResponse<List<GameScoreDto>>> getRankings() {
          List<GameScoreDto> rankings = gameScoreService.getTop10Rankings();
          return ResponseEntity.ok(ApiResponse.success(rankings));
      }

      @GetMapping("/player/{nickname}")
      public ResponseEntity<ApiResponse<List<GameScoreDto>>> getPlayerScores(
              @PathVariable String nickname) {
          List<GameScoreDto> scores = gameScoreService.getPlayerScores(nickname);
          return ResponseEntity.ok(ApiResponse.success(scores));
      }
  }
  ```

**예상 시간**: 6-8시간
**산출물**: GameScoreService, GameScoreController

---

### Day 12: ShopService 및 ShopController
**작업 내용**:
- [ ] **ShopService**
  ```java
  @Service
  @RequiredArgsConstructor
  public class ShopService {
      private final ItemRepository itemRepository;
      private final PlayerItemRepository playerItemRepository;
      private final PlayerRepository playerRepository;

      public List<Item> getAllItems() {
          return itemRepository.findAll();
      }

      public List<Item> getItemsByType(String type) {
          return itemRepository.findByType(type);
      }

      @Transactional
      public PlayerItem purchaseItem(String nickname, Long itemId) {
          Player player = playerRepository.findByNickname(nickname)
                  .orElseThrow(() -> new PlayerNotFoundException("플레이어를 찾을 수 없습니다."));

          Item item = itemRepository.findById(itemId)
                  .orElseThrow(() -> new RuntimeException("아이템을 찾을 수 없습니다."));

          if (player.getGold() < item.getPrice()) {
              throw new InsufficientGoldException("골드가 부족합니다.");
          }

          // 골드 차감
          player.setGold(player.getGold() - item.getPrice());

          // 스탯 적용
          player.setAtk(player.getAtk() + item.getAtkBonus());
          player.setMaxHp(player.getMaxHp() + item.getHpBonus());
          player.setHp(player.getMaxHp()); // HP 최대치로 회복

          playerRepository.save(player);

          // 인벤토리에 추가
          PlayerItem playerItem = new PlayerItem();
          playerItem.setPlayer(player);
          playerItem.setItem(item);

          return playerItemRepository.save(playerItem);
      }

      public List<PlayerItem> getPlayerInventory(String nickname) {
          Player player = playerRepository.findByNickname(nickname)
                  .orElseThrow(() -> new PlayerNotFoundException("플레이어를 찾을 수 없습니다."));

          return playerItemRepository.findByPlayerId(player.getId());
      }
  }
  ```

- [ ] **DTO 클래스**
  ```java
  @Data
  public class ItemDto {
      private Long id;
      private String name;
      private String type;
      private Integer price;
      private Integer atkBonus;
      private Integer hpBonus;
      private String description;
      private String icon;
  }

  @Data
  public class PurchaseRequest {
      @NotNull
      private Long itemId;
  }
  ```

- [ ] **ShopController**
  ```java
  @RestController
  @RequestMapping("/api/shop")
  @RequiredArgsConstructor
  public class ShopController {
      private final ShopService shopService;

      @GetMapping("/items")
      public ResponseEntity<ApiResponse<List<ItemDto>>> getAllItems() {
          List<Item> items = shopService.getAllItems();
          List<ItemDto> dtos = items.stream()
                  .map(this::convertToDto)
                  .collect(Collectors.toList());
          return ResponseEntity.ok(ApiResponse.success(dtos));
      }

      @GetMapping("/items/type/{type}")
      public ResponseEntity<ApiResponse<List<ItemDto>>> getItemsByType(@PathVariable String type) {
          List<Item> items = shopService.getItemsByType(type);
          List<ItemDto> dtos = items.stream()
                  .map(this::convertToDto)
                  .collect(Collectors.toList());
          return ResponseEntity.ok(ApiResponse.success(dtos));
      }

      @PostMapping("/purchase/{nickname}")
      public ResponseEntity<ApiResponse<String>> purchaseItem(
              @PathVariable String nickname,
              @Valid @RequestBody PurchaseRequest request) {
          shopService.purchaseItem(nickname, request.getItemId());
          return ResponseEntity.ok(ApiResponse.success("구매 완료"));
      }

      @GetMapping("/inventory/{nickname}")
      public ResponseEntity<ApiResponse<List<ItemDto>>> getInventory(@PathVariable String nickname) {
          List<PlayerItem> inventory = shopService.getPlayerInventory(nickname);
          List<ItemDto> dtos = inventory.stream()
                  .map(pi -> convertToDto(pi.getItem()))
                  .collect(Collectors.toList());
          return ResponseEntity.ok(ApiResponse.success(dtos));
      }

      private ItemDto convertToDto(Item item) {
          ItemDto dto = new ItemDto();
          dto.setId(item.getId());
          dto.setName(item.getName());
          dto.setType(item.getType());
          dto.setPrice(item.getPrice());
          dto.setAtkBonus(item.getAtkBonus());
          dto.setHpBonus(item.getHpBonus());
          dto.setDescription(item.getDescription());
          dto.setIcon(item.getIcon());
          return dto;
      }
  }
  ```

- [ ] Postman으로 API 테스트
  - GET `/api/shop/items`
  - POST `/api/shop/purchase/TestPlayer`
  - GET `/api/shop/inventory/TestPlayer`

**예상 시간**: 6-8시간
**산출물**: ShopService, ShopController, API 테스트 완료

---

## 🔗 Phase 5: 프론트엔드-백엔드 연동 (Day 13-15)

**목표**: 프론트엔드에서 백엔드 API 호출

### Day 13: API 클라이언트 작성
**작업 내용**:
- [ ] 환경 변수 설정
  ```bash
  # .env.local (개발용)
  VITE_API_URL=http://localhost:8080/api

  # .env.production (배포용)
  VITE_API_URL=https://your-backend.onrender.com/api
  ```

- [ ] **API 클라이언트 (src/api/client.js)**
  ```javascript
  const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8080/api';

  class ApiClient {
    async request(endpoint, options = {}) {
      const url = `${API_BASE_URL}${endpoint}`;
      const config = {
        headers: {
          'Content-Type': 'application/json',
          ...options.headers,
        },
        ...options,
      };

      try {
        const response = await fetch(url, config);
        const data = await response.json();

        if (!data.success) {
          throw new Error(data.message || '요청 실패');
        }

        return data.data;
      } catch (error) {
        console.error('API Error:', error);
        throw error;
      }
    }

    // Player API
    async getPlayer(nickname) {
      return this.request(`/players/${nickname}`);
    }

    async updatePlayerStats(nickname, exp, gold) {
      return this.request(`/players/${nickname}/update-stats`, {
        method: 'POST',
        body: JSON.stringify({ exp, gold }),
      });
    }

    // Score API
    async saveScore(nickname, scoreData) {
      return this.request(`/scores/${nickname}`, {
        method: 'POST',
        body: JSON.stringify(scoreData),
      });
    }

    async getRankings() {
      return this.request('/scores/rankings');
    }

    async getPlayerScores(nickname) {
      return this.request(`/scores/player/${nickname}`);
    }

    // Shop API
    async getItems() {
      return this.request('/shop/items');
    }

    async getItemsByType(type) {
      return this.request(`/shop/items/type/${type}`);
    }

    async purchaseItem(nickname, itemId) {
      return this.request(`/shop/purchase/${nickname}`, {
        method: 'POST',
        body: JSON.stringify({ itemId }),
      });
    }

    async getInventory(nickname) {
      return this.request(`/shop/inventory/${nickname}`);
    }
  }

  export const api = new ApiClient();
  ```

**예상 시간**: 4-5시간
**산출물**: API 클라이언트

---

### Day 14: 닉네임 시스템 및 GameContext 수정
**작업 내용**:
- [ ] **NicknameModal 컴포넌트**
  ```javascript
  import { useState } from 'react';
  import './NicknameModal.css';

  const NicknameModal = ({ onSubmit }) => {
    const [nickname, setNickname] = useState('');
    const [error, setError] = useState('');

    const handleSubmit = () => {
      if (!nickname.trim()) {
        setError('닉네임을 입력해주세요.');
        return;
      }

      if (nickname.length < 2 || nickname.length > 12) {
        setError('닉네임은 2~12자로 입력해주세요.');
        return;
      }

      localStorage.setItem('typingRpg_nickname', nickname);
      onSubmit(nickname);
    };

    return (
      <div className="modal-overlay">
        <div className="modal-content">
          <h2>🎮 TyperRPG</h2>
          <p>닉네임을 입력하고 모험을 시작하세요!</p>
          <input
            type="text"
            value={nickname}
            onChange={(e) => setNickname(e.target.value)}
            placeholder="닉네임 (2~12자)"
            maxLength={12}
            onKeyPress={(e) => e.key === 'Enter' && handleSubmit()}
          />
          {error && <p className="error">{error}</p>}
          <button onClick={handleSubmit}>시작하기</button>
        </div>
      </div>
    );
  };

  export default NicknameModal;
  ```

- [ ] **GameContext 수정 (API 연동)**
  ```javascript
  import { createContext, useState, useEffect } from 'react';
  import { api } from '../api/client';

  export const GameContext = createContext();

  export const GameProvider = ({ children }) => {
    const [player, setPlayer] = useState(null);
    const [nickname, setNickname] = useState(null);
    const [loading, setLoading] = useState(true);

    useEffect(() => {
      const savedNickname = localStorage.getItem('typingRpg_nickname');
      if (savedNickname) {
        loadPlayer(savedNickname);
      } else {
        setLoading(false);
      }
    }, []);

    const loadPlayer = async (nick) => {
      try {
        setLoading(true);
        const data = await api.getPlayer(nick);
        setPlayer(data);
        setNickname(nick);
      } catch (error) {
        console.error('Failed to load player:', error);
      } finally {
        setLoading(false);
      }
    };

    const updateStats = async (exp, gold) => {
      try {
        const updated = await api.updatePlayerStats(nickname, exp, gold);
        setPlayer(updated);
        return updated;
      } catch (error) {
        console.error('Failed to update stats:', error);
        throw error;
      }
    };

    const saveGameScore = async (scoreData) => {
      try {
        await api.saveScore(nickname, scoreData);
      } catch (error) {
        console.error('Failed to save score:', error);
        throw error;
      }
    };

    return (
      <GameContext.Provider value={{
        player,
        nickname,
        loading,
        setNickname: loadPlayer,
        updateStats,
        saveGameScore,
      }}>
        {children}
      </GameContext.Provider>
    );
  };
  ```

- [ ] **App.jsx에 NicknameModal 추가**
  ```javascript
  function App() {
    const { nickname, loading } = useContext(GameContext);
    const [showNicknameModal, setShowNicknameModal] = useState(false);

    useEffect(() => {
      if (!loading && !nickname) {
        setShowNicknameModal(true);
      }
    }, [loading, nickname]);

    if (loading) {
      return <div className="loading">로딩 중...</div>;
    }

    return (
      <>
        {showNicknameModal && (
          <NicknameModal onSubmit={() => setShowNicknameModal(false)} />
        )}
        <Router>
          {/* 기존 라우팅 */}
        </Router>
      </>
    );
  }
  ```

**예상 시간**: 6-8시간
**산출물**: 닉네임 시스템, GameContext API 연동

---

### Day 15: 페이지별 API 연동
**작업 내용**:
- [ ] **GameResult 컴포넌트 수정**
  ```javascript
  const handleSaveScore = async () => {
    try {
      const scoreData = {
        contentId: selectedContent.id,
        score: finalScore,
        wpm: finalWpm,
        accuracy: finalAccuracy,
        monstersDefeated: 1,
        isWin: isWin,
      };

      await saveGameScore(scoreData);

      if (isWin) {
        await updateStats(content.monster.exp, content.monster.gold);
      }

      alert('점수가 저장되었습니다!');
      navigate('/ranking');
    } catch (error) {
      alert('점수 저장에 실패했습니다.');
    }
  };
  ```

- [ ] **Ranking 페이지 수정**
  ```javascript
  const [rankings, setRankings] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const loadRankings = async () => {
      try {
        const data = await api.getRankings();
        setRankings(data);
      } catch (error) {
        console.error('Failed to load rankings:', error);
      } finally {
        setLoading(false);
      }
    };

    loadRankings();
  }, []);
  ```

- [ ] **Shop 페이지 수정**
  ```javascript
  const handlePurchase = async (itemId) => {
    try {
      await api.purchaseItem(nickname, itemId);
      alert('구매 완료!');

      // 플레이어 정보 새로고침
      const updated = await api.getPlayer(nickname);
      setPlayer(updated);
    } catch (error) {
      alert(error.message || '구매에 실패했습니다.');
    }
  };
  ```

- [ ] **Profile 페이지 수정**
  ```javascript
  const [playerScores, setPlayerScores] = useState([]);

  useEffect(() => {
    const loadScores = async () => {
      try {
        const data = await api.getPlayerScores(nickname);
        setPlayerScores(data);
      } catch (error) {
        console.error('Failed to load scores:', error);
      }
    };

    if (nickname) {
      loadScores();
    }
  }, [nickname]);
  ```

- [ ] LocalStorage 코드 제거
  - storage.js의 사용을 모두 API 호출로 변경
  - localStorage는 닉네임 저장 용도로만 사용

**예상 시간**: 6-8시간
**산출물**: 모든 페이지 API 연동 완료

---

## 🚀 Phase 6: 백엔드 배포 및 통합 테스트 (Day 16-18)

**목표**: Render.com에 백엔드 배포 및 전체 시스템 테스트

### Day 16: Render.com 배포 준비
**작업 내용**:
- [ ] Render.com 회원가입
  - https://render.com 접속
  - GitHub 계정으로 가입

- [ ] GitHub Repository 연결
  - 백엔드 코드를 GitHub에 push
  - Render.com에서 Repository 연결

- [ ] `build.gradle` 확인
  ```groovy
  tasks.named('jar') {
      enabled = false
  }

  tasks.named('bootJar') {
      enabled = true
      archiveFileName = 'app.jar'
  }
  ```

- [ ] 배포용 `application.yml` 설정
  ```yaml
  server:
    port: ${PORT:8080}

  spring:
    datasource:
      url: jdbc:sqlite:/data/typingrpg.db
      driver-class-name: org.sqlite.JDBC
    jpa:
      database-platform: org.hibernate.community.dialect.SQLiteDialect
      hibernate:
        ddl-auto: update
      show-sql: false
  ```

**예상 시간**: 3-4시간
**산출물**: 배포 준비 완료

---

### Day 17: Render.com 배포 실행
**작업 내용**:
- [ ] Web Service 생성
  - Service Name: `typer-rpg-backend`
  - Environment: `Java 17`
  - Build Command: `./gradlew clean bootJar`
  - Start Command: `java -jar build/libs/app.jar`
  - Instance Type: Free

- [ ] 환경 변수 설정
  - `PORT`: 8080 (자동 설정됨)

- [ ] Disk 추가 (SQLite 파일 저장용)
  - Disk Name: `data`
  - Mount Path: `/data`
  - Size: 1GB (무료)

- [ ] 배포 실행 및 로그 확인
  - 빌드 로그 확인
  - 애플리케이션 시작 로그 확인
  - 헬스체크 API 테스트: `https://your-backend.onrender.com/api/health`

- [ ] 초기 데이터 확인
  - `/api/shop/items` 호출하여 아이템 8개 확인

**예상 시간**: 4-5시간
**산출물**: Render.com 배포 완료, 공개 API URL

---

### Day 18: 통합 테스트 및 프론트엔드 배포
**작업 내용**:
- [ ] 프론트엔드 환경 변수 설정
  ```bash
  # .env.production
  VITE_API_URL=https://your-backend.onrender.com/api
  ```

- [ ] GitHub Pages 재배포
  ```bash
  npm run build
  gh-pages -d dist
  ```

- [ ] 통합 테스트 (Production 환경)
  - [ ] 닉네임 입력 → 플레이어 생성 확인
  - [ ] 게임 플레이 → 점수 저장 확인
  - [ ] 랭킹 페이지에서 API 데이터 확인
  - [ ] 상점 구매 → 골드 차감, 스탯 증가 확인
  - [ ] 프로필 페이지에서 게임 기록 확인
  - [ ] 새로고침 후 데이터 유지 확인

- [ ] CORS 에러 확인 및 수정
  - 프론트엔드 URL을 CORS allowedOrigins에 추가

- [ ] 성능 테스트
  - API 응답 속도 확인
  - 콜드 스타트 시간 확인 (15분 미사용 후 첫 요청)

- [ ] 버그 수정
  - 발견된 버그 즉시 수정 및 재배포

**예상 시간**: 6-8시간
**산출물**: 완전히 작동하는 Full Stack 애플리케이션

---

## 🎮 Phase 7: 게임 기능 확장 (Day 19-21)

**목표**: 게임 모드 추가 및 콘텐츠 확장

### Day 19: 다양한 게임 모드 추가
**작업 내용**:
- [ ] 게임 모드 데이터 구조
  ```javascript
  export const gameModes = {
    NORMAL: {
      id: 'normal',
      name: '무한 타이핑',
      description: '60초 동안 문장을 타이핑하세요',
      timeLimit: 60,
      hasMonster: true,
    },
    BOSS: {
      id: 'boss',
      name: '보스 배틀',
      description: '강력한 보스를 처치하세요',
      timeLimit: 90,
      hasMonster: true,
    },
    ENDLESS: {
      id: 'endless',
      name: '엔드리스 모드',
      description: 'HP가 0이 될 때까지 계속하세요',
      timeLimit: null,
      hasMonster: true,
    },
    SPEED: {
      id: 'speed',
      name: '스피드 모드',
      description: '30초 안에 최대한 많이 입력하세요',
      timeLimit: 30,
      hasMonster: false,
    },
  };
  ```

- [ ] ContentSelect 페이지에 모드 선택 추가
  - "무한 타이핑", "보스 배틀", "엔드리스", "스피드" 버튼
  - 각 모드의 설명 표시

- [ ] GamePlay 컴포넌트에 모드별 로직 추가
  - 엔드리스 모드: 타이머 없음, HP 0까지 진행
  - 스피드 모드: 몬스터 없음, 30초 타이머

- [ ] 모드별 점수 계산
  - 엔드리스: 생존 시간 × 정확도
  - 스피드: 타이핑한 문자 수 × WPM

**예상 시간**: 6-8시간
**산출물**: 4가지 게임 모드

---

### Day 20: 난이도 시스템 및 콘텐츠 확장
**작업 내용**:
- [ ] 난이도별 몬스터 HP 조정
  ```javascript
  const difficultyModifiers = {
    easy: 0.7,      // HP 70%
    normal: 1.0,    // HP 100%
    hard: 1.5,      // HP 150%
    'very-hard': 2.0, // HP 200%
  };
  ```

- [ ] 스테이지 5개 → 10개로 확장
  ```javascript
  // sentences.js에 추가
  {
    id: 6,
    title: '얼음 성',
    difficulty: 'hard',
    description: '얼음으로 뒤덮인 성입니다.',
    recommendedWPM: 80,
    monsterId: 3
  },
  {
    id: 7,
    title: '마법의 숲',
    difficulty: 'normal',
    description: '마법이 깃든 신비한 숲입니다.',
    recommendedWPM: 55,
    monsterId: 2
  },
  // ... 총 10개
  ```

- [ ] 카테고리별 스테이지 추가
  - 코딩 스테이지: 코딩 카테고리 문장만 사용
  - 명언 스테이지: 명언 카테고리 문장만 사용

- [ ] 랜덤 스테이지 기능
  - "랜덤 도전" 버튼 추가
  - 매번 다른 카테고리 및 난이도 문장 출제

**예상 시간**: 6-8시간
**산출물**: 10개 스테이지, 난이도 시스템

---

### Day 21: 업적 시스템
**작업 내용**:
- [ ] 업적 데이터 구조
  ```javascript
  export const achievements = [
    {
      id: 1,
      title: '첫 걸음',
      description: '첫 게임을 완료하세요',
      icon: '🎯',
      condition: (stats) => stats.gamesPlayed >= 1,
      reward: { exp: 10, gold: 50 }
    },
    {
      id: 2,
      title: '타자왕',
      description: 'WPM 100 달성',
      icon: '⚡',
      condition: (stats) => stats.maxWpm >= 100,
      reward: { exp: 50, gold: 200 }
    },
    {
      id: 3,
      title: '부자',
      description: '골드 1000 획득',
      icon: '💰',
      condition: (stats) => stats.gold >= 1000,
      reward: { exp: 30, gold: 100 }
    },
    {
      id: 4,
      title: '완벽주의자',
      description: '정확도 100% 달성',
      icon: '💯',
      condition: (stats) => stats.maxAccuracy >= 100,
      reward: { exp: 100, gold: 500 }
    },
    {
      id: 5,
      title: '승리의 연속',
      description: '연속 5승',
      icon: '🔥',
      condition: (stats) => stats.winStreak >= 5,
      reward: { exp: 80, gold: 300 }
    },
  ];
  ```

- [ ] 업적 달성 체크 로직
  ```javascript
  const checkAchievements = (playerStats) => {
    const newAchievements = [];
    const unlocked = JSON.parse(localStorage.getItem('achievements') || '[]');

    achievements.forEach(ach => {
      if (!unlocked.includes(ach.id) && ach.condition(playerStats)) {
        newAchievements.push(ach);
        unlocked.push(ach.id);
      }
    });

    if (newAchievements.length > 0) {
      localStorage.setItem('achievements', JSON.stringify(unlocked));
      showAchievementToast(newAchievements);
    }
  };
  ```

- [ ] 업적 알림 토스트
  ```javascript
  const AchievementToast = ({ achievement }) => {
    return (
      <div className="achievement-toast">
        <div className="achievement-icon">{achievement.icon}</div>
        <div className="achievement-info">
          <h4>🎉 업적 달성!</h4>
          <p>{achievement.title}</p>
          <small>+{achievement.reward.exp} EXP, +{achievement.reward.gold} Gold</small>
        </div>
      </div>
    );
  };
  ```

- [ ] 업적 페이지 (`/achievements`)
  - 달성한 업적 / 미달성 업적 표시
  - 진행도 표시 (예: "5/10 게임 플레이")

**예상 시간**: 6-8시간
**산출물**: 업적 시스템, 업적 페이지

---

## 🎨 Phase 8: UI/UX 개선 및 리소스 (Day 22-24)

**목표**: 픽셀 아트 적용 및 애니메이션

### Day 22: 무료 픽셀 아트 리소스 적용
**작업 내용**:
- [ ] 무료 리소스 다운로드
  - **itch.io**: https://itch.io/game-assets/free/tag-pixel-art
  - **OpenGameArt.org**: https://opengameart.org/
  - **Kenney.nl**: https://kenney.nl/assets

- [ ] 필요한 리소스 목록
  - 플레이어 스프라이트 (32x32 또는 64x64)
    - Idle 애니메이션 (4프레임)
    - Attack 애니메이션 (4프레임)
  - 몬스터 스프라이트 (5종)
    - 고블린, 늑대, 트롤, 드래곤, 버그
  - 아이템 아이콘 (8개)
  - 배경 이미지 (5-10개)
  - UI 요소 (버튼, 패널, HP 바)

- [ ] 이미지 최적화
  ```bash
  # WebP 변환 (압축률 80%)
  # https://squoosh.app/ 사용
  ```

- [ ] 리소스 폴더 구조
  ```
  public/assets/
  ├── characters/
  │   ├── player-idle.png
  │   └── player-attack.png
  ├── monsters/
  │   ├── goblin.png
  │   ├── wolf.png
  │   └── ...
  ├── items/
  │   ├── wooden-sword.png
  │   └── ...
  ├── backgrounds/
  │   ├── forest.png
  │   └── ...
  └── ui/
      ├── button.png
      └── panel.png
  ```

- [ ] CSS로 리소스 적용
  - 배경 이미지 설정
  - 캐릭터 및 몬스터 스프라이트 표시

**예상 시간**: 6-8시간
**산출물**: 픽셀 아트 리소스 적용

---

### Day 23: 애니메이션 추가
**작업 내용**:
- [ ] 캐릭터 스프라이트 애니메이션
  ```css
  @keyframes player-idle {
    0%, 100% { background-position-x: 0px; }
    25% { background-position-x: -32px; }
    50% { background-position-x: -64px; }
    75% { background-position-x: -96px; }
  }

  .player-sprite {
    width: 32px;
    height: 32px;
    background-image: url('/assets/characters/player-idle.png');
    animation: player-idle 1s steps(4) infinite;
  }
  ```

- [ ] 공격 애니메이션
  ```javascript
  const playAttackAnimation = () => {
    setAnimationState('attack');
    setTimeout(() => {
      setAnimationState('idle');
    }, 500);
  };
  ```

- [ ] 데미지 텍스트 애니메이션
  ```javascript
  const DamageText = ({ damage, x, y }) => {
    return (
      <div
        className="damage-text"
        style={{ left: x, top: y }}
      >
        -{damage}
      </div>
    );
  };
  ```

  ```css
  @keyframes damage-float {
    0% {
      transform: translateY(0);
      opacity: 1;
    }
    100% {
      transform: translateY(-50px);
      opacity: 0;
    }
  }

  .damage-text {
    animation: damage-float 1s ease-out forwards;
  }
  ```

- [ ] 페이지 전환 애니메이션
  ```css
  .page-fade-enter {
    opacity: 0;
  }
  .page-fade-enter-active {
    opacity: 1;
    transition: opacity 300ms;
  }
  .page-fade-exit {
    opacity: 1;
  }
  .page-fade-exit-active {
    opacity: 0;
    transition: opacity 300ms;
  }
  ```

- [ ] 버튼 호버 효과
  ```css
  .button {
    transition: all 0.2s ease;
  }
  .button:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 8px rgba(0,0,0,0.3);
  }
  .button:active {
    transform: scale(0.95);
  }
  ```

**예상 시간**: 6-8시간
**산출물**: 애니메이션 시스템

---

### Day 24: UX 개선
**작업 내용**:
- [ ] 로딩 상태 표시
  ```javascript
  const LoadingSpinner = () => {
    return (
      <div className="loading-overlay">
        <div className="spinner"></div>
        <p>로딩 중...</p>
      </div>
    );
  };
  ```

- [ ] 에러 처리 개선
  ```javascript
  const ErrorBoundary = ({ children }) => {
    const [hasError, setHasError] = useState(false);

    if (hasError) {
      return (
        <div className="error-page">
          <h2>오류가 발생했습니다</h2>
          <button onClick={() => window.location.reload()}>
            새로고침
          </button>
        </div>
      );
    }

    return children;
  };
  ```

- [ ] 튜토리얼 추가
  ```javascript
  const Tutorial = ({ onClose }) => {
    const steps = [
      { title: '게임 방법', content: '문장을 정확하게 입력하세요' },
      { title: '전투 시스템', content: '정답 시 몬스터 데미지, 오답 시 HP 감소' },
      { title: '성장 시스템', content: '경험치를 얻어 레벨업하세요' },
    ];

    return <TutorialCarousel steps={steps} onClose={onClose} />;
  };
  ```

- [ ] 키보드 단축키
  ```javascript
  useEffect(() => {
    const handleKeyDown = (e) => {
      if (e.key === 'Escape') {
        navigate('/');
      }
      if (e.key === 'Enter' && isOnHomePage) {
        navigate('/content');
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, []);
  ```

- [ ] 반응형 알림 (네트워크 오류 등)
  ```javascript
  const Toast = ({ message, type }) => {
    return (
      <div className={`toast toast-${type}`}>
        {message}
      </div>
    );
  };
  ```

**예상 시간**: 6-8시간
**산출물**: 향상된 UX

---

## ⚡ Phase 9: 추가 기능 구현 (Day 25-27)

**목표**: 사운드, 통계, 추가 페이지

### Day 25: 사운드 효과
**작업 내용**:
- [ ] 무료 사운드 리소스 다운로드
  - **Freesound.org**: https://freesound.org/
  - **Zapsplat**: https://www.zapsplat.com/

- [ ] 필요한 사운드
  - 타이핑 사운드 (키 입력)
  - 정답 사운드
  - 오답 사운드
  - 레벨업 사운드
  - 구매 사운드
  - 배경 음악 (선택)

- [ ] 사운드 관리자
  ```javascript
  class SoundManager {
    constructor() {
      this.sounds = {
        typing: new Audio('/assets/sounds/typing.mp3'),
        correct: new Audio('/assets/sounds/correct.mp3'),
        wrong: new Audio('/assets/sounds/wrong.mp3'),
        levelup: new Audio('/assets/sounds/levelup.mp3'),
        purchase: new Audio('/assets/sounds/purchase.mp3'),
      };
      this.enabled = true;
    }

    play(soundName) {
      if (this.enabled && this.sounds[soundName]) {
        this.sounds[soundName].currentTime = 0;
        this.sounds[soundName].play();
      }
    }

    toggle() {
      this.enabled = !this.enabled;
    }
  }

  export const soundManager = new SoundManager();
  ```

- [ ] Settings 페이지에 사운드 ON/OFF 토글
  ```javascript
  const [soundEnabled, setSoundEnabled] = useState(true);

  const toggleSound = () => {
    setSoundEnabled(!soundEnabled);
    soundManager.toggle();
    localStorage.setItem('soundEnabled', !soundEnabled);
  };
  ```

**예상 시간**: 5-6시간
**산출물**: 사운드 시스템

---

### Day 26: 통계 페이지 및 개인 기록
**작업 내용**:
- [ ] Statistics 페이지 (`/statistics`)
  ```javascript
  const Statistics = () => {
    const [stats, setStats] = useState(null);

    useEffect(() => {
      const loadStats = async () => {
        const scores = await api.getPlayerScores(nickname);

        const stats = {
          totalGames: scores.length,
          totalWins: scores.filter(s => s.isWin).length,
          winRate: (scores.filter(s => s.isWin).length / scores.length * 100).toFixed(1),
          maxWpm: Math.max(...scores.map(s => s.wpm)),
          avgWpm: (scores.reduce((sum, s) => sum + s.wpm, 0) / scores.length).toFixed(1),
          maxAccuracy: Math.max(...scores.map(s => s.accuracy)),
          avgAccuracy: (scores.reduce((sum, s) => sum + s.accuracy, 0) / scores.length).toFixed(1),
          totalScore: scores.reduce((sum, s) => sum + s.score, 0),
        };

        setStats(stats);
      };

      loadStats();
    }, [nickname]);

    return (
      <div className="statistics">
        <h2>📊 통계</h2>
        <div className="stats-grid">
          <StatCard title="총 게임 수" value={stats.totalGames} icon="🎮" />
          <StatCard title="승률" value={`${stats.winRate}%`} icon="🏆" />
          <StatCard title="최고 WPM" value={stats.maxWpm} icon="⚡" />
          <StatCard title="평균 WPM" value={stats.avgWpm} icon="📈" />
          <StatCard title="최고 정확도" value={`${stats.maxAccuracy}%`} icon="🎯" />
          <StatCard title="총 점수" value={stats.totalScore} icon="💯" />
        </div>
      </div>
    );
  };
  ```

- [ ] 개인 기록 차트 (Chart.js 사용)
  ```bash
  npm install chart.js react-chartjs-2
  ```

  ```javascript
  import { Line } from 'react-chartjs-2';

  const WpmChart = ({ scores }) => {
    const data = {
      labels: scores.map((_, i) => i + 1),
      datasets: [{
        label: 'WPM',
        data: scores.map(s => s.wpm),
        borderColor: '#4caf50',
        backgroundColor: 'rgba(76, 175, 80, 0.1)',
      }]
    };

    return <Line data={data} />;
  };
  ```

**예상 시간**: 6-8시간
**산출물**: 통계 페이지, 차트

---

### Day 27: 설정 페이지 및 기타 기능
**작업 내용**:
- [ ] Settings 페이지 (`/settings`)
  ```javascript
  const Settings = () => {
    const [settings, setSettings] = useState({
      sound: true,
      showTutorial: true,
      difficulty: 'normal',
      theme: 'dark',
    });

    const handleSave = () => {
      localStorage.setItem('settings', JSON.stringify(settings));
      alert('설정이 저장되었습니다!');
    };

    return (
      <div className="settings">
        <h2>⚙️ 설정</h2>

        <div className="setting-item">
          <label>사운드</label>
          <input
            type="checkbox"
            checked={settings.sound}
            onChange={(e) => setSettings({ ...settings, sound: e.target.checked })}
          />
        </div>

        <div className="setting-item">
          <label>튜토리얼 보기</label>
          <input
            type="checkbox"
            checked={settings.showTutorial}
            onChange={(e) => setSettings({ ...settings, showTutorial: e.target.checked })}
          />
        </div>

        <button onClick={handleSave}>저장</button>
      </div>
    );
  };
  ```

- [ ] 데이터 초기화 기능
  ```javascript
  const handleReset = () => {
    if (confirm('정말 모든 데이터를 초기화하시겠습니까?')) {
      localStorage.clear();
      window.location.reload();
    }
  };
  ```

- [ ] 닉네임 변경 기능
  ```javascript
  const handleChangeNickname = () => {
    const newNickname = prompt('새 닉네임을 입력하세요');
    if (newNickname) {
      localStorage.setItem('typingRpg_nickname', newNickname);
      window.location.reload();
    }
  };
  ```

- [ ] About 페이지
  - 프로젝트 소개
  - 개발자 정보
  - 사용된 기술 스택
  - GitHub 링크

**예상 시간**: 6-8시간
**산출물**: 설정 페이지, 부가 기능

---

## 🏁 Phase 10: 최종 테스트 및 완성 (Day 28-30)

**목표**: 버그 수정, 최종 테스트, 문서화

### Day 28: 전체 기능 테스트
**작업 내용**:
- [ ] **기능별 체크리스트**
  - [ ] 회원 시스템 (닉네임 입력, 플레이어 생성)
  - [ ] 게임 플레이 (4가지 모드)
  - [ ] 스테이지 10개 모두 플레이 가능
  - [ ] 랭킹 시스템 (Top 10)
  - [ ] 상점 (아이템 구매, 스탯 적용)
  - [ ] 프로필 (플레이어 정보, 게임 기록)
  - [ ] 업적 시스템 (달성, 보상)
  - [ ] 통계 페이지 (차트, 기록)
  - [ ] 설정 페이지 (사운드, 튜토리얼)

- [ ] **버그 리스트 작성**
  - 발견된 모든 버그 기록
  - 우선순위 분류 (Critical, High, Medium, Low)

- [ ] **크로스 브라우저 테스트**
  - Chrome (최신 버전)
  - Firefox (최신 버전)
  - Edge (최신 버전)
  - Safari (Mac에서 테스트 가능한 경우)

- [ ] **성능 테스트**
  - Lighthouse 점수 확인
    - Performance: 90+ 목표
    - Accessibility: 90+ 목표
    - Best Practices: 90+ 목표
    - SEO: 80+ 목표

- [ ] **API 부하 테스트**
  - 동시 요청 처리 확인
  - 응답 속도 측정

**예상 시간**: 8시간
**산출물**: 버그 리스트, 테스트 보고서

---

### Day 29: 버그 수정 및 최적화
**작업 내용**:
- [ ] Critical 버그 수정
  - 게임 진행 불가능한 버그
  - 데이터 손실 버그
  - 보안 취약점

- [ ] High 우선순위 버그 수정
  - 주요 기능 오작동
  - UI/UX 문제

- [ ] 성능 최적화
  - 이미지 lazy loading
  ```javascript
  <img src={image} loading="lazy" alt="..." />
  ```

  - 코드 스플리팅
  ```javascript
  const Statistics = lazy(() => import('./pages/Statistics'));
  const Achievements = lazy(() => import('./pages/Achievements'));
  ```

  - 번들 크기 최적화
  ```bash
  npm run build
  # dist 폴더 크기 확인
  ```

- [ ] 접근성 개선
  - ARIA 라벨 추가
  - 키보드 네비게이션 개선
  - 색 대비 확인 (WCAG AA 기준)

**예상 시간**: 8시간
**산출물**: 버그 수정 완료, 최적화된 앱

---

### Day 30: 문서화 및 최종 배포
**작업 내용**:
- [ ] **README.md 작성**
  ```markdown
  # TypingRPG

  타이핑 연습과 RPG 요소를 결합한 게임

  ## 🎮 주요 기능
  - 4가지 게임 모드
  - 10개 스테이지
  - 레벨 시스템 및 상점
  - 업적 및 통계

  ## 🛠 기술 스택
  - Frontend: React 19, Vite, React Router
  - Backend: Spring Boot 3, SQLite, JPA
  - Deployment: GitHub Pages, Render.com

  ## 🚀 실행 방법
  ### 프론트엔드
  ```bash
  cd typing-rpg-frontend
  npm install
  npm run dev
  ```

  ### 백엔드
  ```bash
  cd typing-rpg-backend
  ./gradlew bootRun
  ```

  ## 📸 스크린샷
  [스크린샷 추가]

  ## 🔗 배포 URL
  - Frontend: https://yourusername.github.io/TyperRPG/
  - Backend API: https://your-backend.onrender.com/api

  ## 📝 API 문서
  [API 문서 링크 또는 내용]

  ## 👨‍💻 개발자
  Your Name
  ```

- [ ] **API 문서 작성**
  - Swagger 설정 (선택)
  - 또는 Markdown으로 API 명세 작성

  ```markdown
  # API 문서

  ## Player API
  ### GET /api/players/{nickname}
  플레이어 정보 조회 또는 생성

  **Request:**
  ```
  GET /api/players/TestPlayer
  ```

  **Response:**
  ```json
  {
    "success": true,
    "message": "Success",
    "data": {
      "id": 1,
      "nickname": "TestPlayer",
      "level": 5,
      "exp": 250,
      "gold": 1200,
      ...
    }
  }
  ```
  ```

- [ ] **스크린샷 촬영**
  - 홈 페이지
  - 게임 플레이 화면
  - 랭킹 페이지
  - 상점 페이지
  - 프로필 페이지

- [ ] **최종 배포**
  - 프론트엔드 빌드 및 GitHub Pages 배포
  - 백엔드 Render.com 재배포
  - 배포 URL 확인 및 테스트

- [ ] **프로젝트 완료 보고서 작성**
  ```markdown
  # TyperRPG 프로젝트 완료 보고서

  ## 프로젝트 개요
  - 기간: 30일
  - 목표: Spring Boot 백엔드 포트폴리오용 타이핑 RPG 게임

  ## 완성된 기능
  - [x] 프론트엔드 (React + Vite)
  - [x] 백엔드 (Spring Boot + SQLite)
  - [x] 게임 시스템 (4가지 모드, 10개 스테이지)
  - [x] 업적 및 통계
  - [x] 사운드 및 애니메이션

  ## 주요 성과
  - 전체 코드 라인 수: [추정치]
  - API 엔드포인트: 10개
  - 문장 데이터: 200개
  - 배포 환경: Production

  ## 배운 점
  - Spring Boot REST API 설계
  - React 상태 관리 (Context API)
  - SQLite 데이터베이스 활용
  - 무료 배포 (Render.com, GitHub Pages)

  ## 개선 가능한 점
  - Redis 캐싱 추가
  - WebSocket 실시간 랭킹
  - 모바일 반응형 지원
  ```

**예상 시간**: 8시간
**산출물**: 완성된 문서, 최종 배포, 완료 보고서

---

## 📊 최종 산출물

### 코드
- **프론트엔드**: React 앱 (약 25개 컴포넌트/페이지)
- **백엔드**: Spring Boot API (10개 엔드포인트)
- **데이터**: 문장 200개, 아이템 8개, 스테이지 10개, 업적 5개+

### 배포
- **프론트엔드**: GitHub Pages (https://yourusername.github.io/TyperRPG/)
- **백엔드**: Render.com (https://your-backend.onrender.com/api)

### 문서
- **README.md**: 프로젝트 소개 및 실행 방법
- **API 문서**: REST API 명세
- **프로젝트 완료 보고서**: 30일 개발 과정 정리

---

## ✅ Phase별 체크리스트

### Phase 1 (Day 1-3) - 프론트엔드 마무리
- [ ] CSS 스타일링 완성
- [ ] 성능 최적화
- [ ] GitHub Pages 배포

### Phase 2 (Day 4-6) - Spring Boot 설정
- [x] 프로젝트 생성
- [x] SQLite 설정
- [x] 예외 처리

### Phase 3 (Day 7-9) - 데이터베이스
- [x] 엔티티 작성
- [x] Repository 작성
- [x] 초기 데이터

### Phase 4 (Day 10-12) - 백엔드 API
- [x] PlayerService & Controller
- [x] GameScoreService & Controller
- [x] ShopService & Controller

### Phase 5 (Day 13-15) - 프론트-백엔드 연동
- [x] API 클라이언트
- [x] 닉네임 시스템
- [x] 페이지별 API 연동

### Phase 6 (Day 16-18) - 배포 및 테스트
- [ ] Render.com 배포
- [ ] 통합 테스트
- [ ] 버그 수정

### Phase 7 (Day 19-21) - 게임 기능 확장
- [ ] 게임 모드 4가지
- [ ] 스테이지 10개
- [ ] 업적 시스템

### Phase 8 (Day 22-24) - UI/UX
- [ ] 픽셀 아트 적용
- [ ] 애니메이션
- [ ] UX 개선

### Phase 9 (Day 25-27) - 추가 기능
- [ ] 사운드 시스템
- [ ] 통계 페이지
- [ ] 설정 페이지

### Phase 10 (Day 28-30) - 최종 완성
- [ ] 전체 테스트
- [ ] 버그 수정
- [ ] 문서화 및 최종 배포

---

## 📅 일정 관리 팁

### 매일 해야 할 것
1. **계획 확인** (아침): 오늘의 목표 확인
2. **작업 시작**: 집중해서 구현
3. **테스트**: 구현한 기능 즉시 테스트
4. **커밋**: 작업 단위로 Git 커밋
5. **정리** (저녁): 완료/미완료 체크, 다음 날 계획

### 주의 사항
- **시간 초과 방지**: 하루 작업이 8시간을 넘지 않도록 관리
- **완벽주의 지양**: 80% 완성도로 다음 단계 진행 (나중에 개선 가능)
- **막힐 때**: 1시간 이상 막히면 다른 작업으로 전환 또는 질문
- **커밋 습관**: 기능 단위로 작은 커밋 (하루 최소 3-5개 커밋)

---

**프로젝트 시작일**: [입력]
**예상 완료일**: [시작일 + 30일]
**현재 Phase**: Phase 1 (Day 1-3)
**다음 작업**: CSS 스타일링 완성
