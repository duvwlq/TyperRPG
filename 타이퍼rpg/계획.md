# TypingRPG 개발 계획서

## 📋 프로젝트 개요

### 목표
Spring Boot 백엔드 포트폴리오를 위한 **경량 타이핑 RPG 게임** 개발

### 핵심 가치
- ✅ **가벼움:** 불필요한 기능 최소화, 빠른 로딩
- ✅ **점진적 개발:** Phase별로 기능 추가
- ✅ **포트폴리오:** Spring Boot 백엔드 역량 증명
- ✅ **무료 운영:** 예산 제로로 배포 및 운영

---

## 🛠 기술 스택

### 프론트엔드
- **Framework:** React 18 + Vite
- **Language:** JavaScript (TypeScript 선택 가능)
- **Styling:** Vanilla CSS (CSS Modules 또는 Styled Components 선택 가능)
- **State Management:** React Context API (Redux 제외)
- **Storage:** LocalStorage → 추후 Backend API
- **Build Tool:** Vite
- **Deployment:** GitHub Pages

### 백엔드
- **Framework:** Spring Boot 3.x
- **Language:** Java 17+
- **Database:** SQLite (경량, 파일 기반)
- **ORM:** Spring Data JPA
- **API:** RESTful API
- **Build Tool:** Gradle
- **Deployment:** Render.com (무료 티어)

### 개발 도구
- **Version Control:** Git + GitHub
- **API Testing:** Postman 또는 Thunder Client
- **Database Viewer:** DB Browser for SQLite

---

## ❌ 제외된 기능 (경량화를 위해)

### Phase 1-2에서 제외
- ❌ 로그인/인증 시스템 (OAuth, JWT)
- ❌ Redis 캐싱
- ❌ 복잡한 애니메이션 (패럴랙스, 파티클)
- ❌ 픽셀 아트 리소스 (나중에 무료 리소스 추가)
- ❌ 모바일 반응형 (PC 전용)
- ❌ 관리자 페이지
- ❌ Google OAuth
- ❌ 이메일 인증

### 간소화된 기능
- 랭킹: 일일/주간 → **전체 랭킹만**
- 콘텐츠: 10+ 문장 → **3-5개 문장 세트**
- 상점: 복잡한 아이템 → **기본 아이템만**

---

## ✅ 포함된 핵심 기능

### 1. 타이핑 게임 코어
- 문장 표시 및 실시간 입력 처리
- WPM (Words Per Minute) 계산
- 정확도 (Accuracy) 계산
- 타이핑 오류 시각적 표시 (정답: 녹색, 오답: 빨간색)

### 2. RPG 요소
- 플레이어 HP
- 몬스터 HP
- 정답 시 몬스터 데미지
- 오답 시 플레이어 HP 감소
- 몬스터 처치 시 경험치 획득

### 3. 랭킹 시스템
- 닉네임 기반 전체 랭킹
- 점수 (Score), WPM, 정확도 표시
- 상위 10명 표시

### 4. 캐릭터 시스템
- 레벨 시스템
- 경험치 (EXP)
- 골드 획득
- 기본 스탯 (HP, ATK)

### 5. 상점 시스템
- 무기 구매 (ATK 증가)
- 방어구 구매 (HP 증가)
- 골드로 구매

### 6. UI 화면
- 메인 페이지 (Start 버튼)
- 콘텐츠 선택 페이지
- 게임 플레이 화면
- 결과 화면
- 랭킹 페이지
- 상점 페이지
- 프로필 페이지

---

## 📅 Phase별 상세 개발 계획

---

## 🎯 Phase 1: 프론트엔드 MVP (2주)

**목표:** 브라우저에서 작동하는 타이핑 게임 + LocalStorage 저장

### Week 1: 프로젝트 세팅 및 코어 기능

#### Day 1-2: 프로젝트 초기 세팅
**작업 내용:**
- [ ] GitHub Repository 생성
  - Repository 이름: `typing-rpg`
  - README.md 작성 (프로젝트 소개)
  - .gitignore 설정 (node_modules, dist, .env)
- [ ] React + Vite 프로젝트 생성
  ```bash
  npm create vite@latest typing-rpg-frontend -- --template react
  cd typing-rpg-frontend
  npm install
  npm run dev
  ```
- [ ] 폴더 구조 설정
  ```
  src/
  ├── components/       # UI 컴포넌트
  ├── pages/           # 페이지 컴포넌트
  ├── hooks/           # Custom Hooks
  ├── utils/           # 유틸리티 함수
  ├── context/         # React Context
  ├── styles/          # CSS 파일
  ├── data/            # 정적 데이터 (문장 목록)
  └── App.jsx
  ```
- [ ] 기본 라우팅 설정 (React Router 설치)
  ```bash
  npm install react-router-dom
  ```
- [ ] 기본 CSS 스타일 세팅
  - 글로벌 스타일 (reset.css)
  - 색상 변수 정의 (픽셀 아트 스타일 색상)
  - 폰트 설정 (시스템 폰트, 나중에 픽셀 폰트로 교체)

**산출물:**
- GitHub Repository
- 실행 가능한 React 프로젝트
- 기본 폴더 구조

---

#### Day 3-4: 타이핑 엔진 구현
**작업 내용:**
- [ ] 타이핑 데이터 구조 설계
  ```javascript
  // src/data/sentences.js
  const sentenceSets = [
    {
      id: 1,
      title: "고블린 숲",
      difficulty: "easy",
      sentences: [
        "안녕하세요",
        "타이핑 연습을 시작합니다",
        "빠르고 정확하게 입력하세요"
      ],
      monster: {
        name: "고블린",
        hp: 100,
        exp: 30,
        gold: 50
      }
    }
    // ... 총 3-5개
  ];
  ```

- [ ] 타이핑 로직 구현 (`src/utils/typingEngine.js`)
  - `calculateWPM(chars, timeInSeconds)` - WPM 계산
  - `calculateAccuracy(correctChars, totalChars)` - 정확도 계산
  - `processKeyInput(input, target)` - 입력 처리 및 검증
  - `highlightText(input, target)` - 정답/오답 하이라이트

- [ ] Custom Hook 작성 (`src/hooks/useTyping.js`)
  ```javascript
  const useTyping = (targetText) => {
    const [input, setInput] = useState('');
    const [startTime, setStartTime] = useState(null);
    const [wpm, setWpm] = useState(0);
    const [accuracy, setAccuracy] = useState(100);
    const [errors, setErrors] = useState(0);

    // 타이핑 로직
    const handleKeyPress = (key) => { ... };

    return { input, wpm, accuracy, errors, handleKeyPress };
  };
  ```

**산출물:**
- 타이핑 엔진 유틸리티 함수
- useTyping Hook
- 문장 데이터 (3-5개 세트)

---

#### Day 5-7: 게임 플레이 화면 구현
**작업 내용:**
- [ ] GamePlay 컴포넌트 작성 (`src/pages/GamePlay.jsx`)
  - 상단 HUD (WPM, Accuracy, Errors)
  - 타이핑 입력 영역
  - 진행 상태 표시

- [ ] RPG 요소 구현
  - 플레이어 HP 바
  - 몬스터 HP 바
  - 데미지 계산 로직
    - 정답: 몬스터 HP -10
    - 오답: 플레이어 HP -5
  - 몬스터 처치 시 보상 (EXP, Gold)

- [ ] 게임 상태 관리 (`src/context/GameContext.jsx`)
  ```javascript
  const GameContext = createContext();

  const GameProvider = ({ children }) => {
    const [player, setPlayer] = useState({
      hp: 100,
      maxHp: 100,
      level: 1,
      exp: 0,
      gold: 0,
      atk: 10
    });

    const [monster, setMonster] = useState(null);

    // 게임 로직
    const attackMonster = (damage) => { ... };
    const takeDamage = (damage) => { ... };
    const gainReward = (exp, gold) => { ... };

    return (
      <GameContext.Provider value={{ ... }}>
        {children}
      </GameContext.Provider>
    );
  };
  ```

- [ ] 게임 종료 조건
  - 몬스터 HP 0 → 승리
  - 플레이어 HP 0 → 패배
  - 모든 문장 완료 → 승리

- [ ] 결과 화면 (`src/components/GameResult.jsx`)
  - 최종 점수
  - WPM, 정확도
  - 획득 경험치, 골드
  - 다시 하기, 메인으로 버튼

**산출물:**
- 완전히 작동하는 게임 플레이 화면
- RPG 전투 시스템
- 게임 결과 화면

**테스트:**
- [ ] 타이핑 입력이 정확히 인식되는가?
- [ ] WPM이 정확히 계산되는가?
- [ ] HP가 정상적으로 감소하는가?
- [ ] 게임 종료 시 결과가 표시되는가?

---

### Week 2: UI 완성 및 LocalStorage 연동

#### Day 8-9: 메인 페이지 및 콘텐츠 선택
**작업 내용:**
- [ ] 메인 페이지 (`src/pages/Home.jsx`)
  - 큰 제목: "LET'S GO ON AN ADVENTURE!"
  - Start 버튼 (콘텐츠 선택으로 이동)
  - 간단한 설명 문구
  - 하단 연락처 (선택 사항)

- [ ] 콘텐츠 선택 페이지 (`src/pages/ContentSelect.jsx`)
  - 좌측: 콘텐츠 목록 (3-5개)
  - 우측: 선택된 콘텐츠 상세 정보
    - 제목
    - 난이도
    - 권장 WPM
    - 보상 (EXP, Gold)
    - GO 버튼

- [ ] 네비게이션 바 (`src/components/Navbar.jsx`)
  - Home
  - Ranking
  - Shop
  - Profile

**산출물:**
- 메인 페이지
- 콘텐츠 선택 페이지
- 네비게이션

---

#### Day 10-11: 랭킹 및 프로필 페이지
**작업 내용:**
- [ ] 랭킹 페이지 (`src/pages/Ranking.jsx`)
  - 전체 랭킹 (LocalStorage)
  - 표시 정보: 순위, 닉네임, 점수, WPM, 정확도
  - 상위 10명만 표시

- [ ] 프로필 페이지 (`src/pages/Profile.jsx`)
  - 캐릭터 정보
    - 닉네임
    - 레벨
    - 경험치 바
    - HP, ATK 스탯
  - 보유 골드
  - 장비 슬롯 (비어있음, Phase 3에서 구현)

- [ ] LocalStorage 관리 (`src/utils/storage.js`)
  ```javascript
  // 플레이어 데이터 저장
  export const savePlayerData = (data) => {
    localStorage.setItem('typingRpg_player', JSON.stringify(data));
  };

  // 플레이어 데이터 불러오기
  export const loadPlayerData = () => {
    const data = localStorage.getItem('typingRpg_player');
    return data ? JSON.parse(data) : null;
  };

  // 랭킹 데이터 저장
  export const saveRanking = (score) => {
    const rankings = loadRankings();
    rankings.push(score);
    rankings.sort((a, b) => b.score - a.score);
    const top10 = rankings.slice(0, 10);
    localStorage.setItem('typingRpg_rankings', JSON.stringify(top10));
  };

  // 랭킹 데이터 불러오기
  export const loadRankings = () => {
    const data = localStorage.getItem('typingRpg_rankings');
    return data ? JSON.parse(data) : [];
  };
  ```

**산출물:**
- 랭킹 페이지
- 프로필 페이지
- LocalStorage 유틸리티

---

#### Day 12-13: 상점 페이지 및 아이템 시스템
**작업 내용:**
- [ ] 상점 데이터 구조 (`src/data/items.js`)
  ```javascript
  const shopItems = [
    {
      id: 1,
      name: "나무 검",
      type: "weapon",
      price: 100,
      stats: { atk: +5 }
    },
    {
      id: 2,
      name: "가죽 갑옷",
      type: "armor",
      price: 150,
      stats: { maxHp: +20 }
    }
    // ... 총 6-8개
  ];
  ```

- [ ] 상점 페이지 (`src/pages/Shop.jsx`)
  - 좌측: Weapon / Armor 탭
  - 우측: 아이템 그리드 (2x3 또는 2x4)
  - 아이템 카드: 이름, 가격, 효과
  - 구매 버튼 (골드 부족 시 비활성화)

- [ ] 구매 로직
  - 골드 차감
  - 아이템 인벤토리에 추가
  - 스탯 자동 적용

- [ ] 인벤토리 시스템 (GameContext에 추가)
  ```javascript
  const [inventory, setInventory] = useState([]);

  const buyItem = (item) => {
    if (player.gold >= item.price) {
      setPlayer(prev => ({
        ...prev,
        gold: prev.gold - item.price,
        ...item.stats  // 스탯 적용
      }));
      setInventory(prev => [...prev, item]);
    }
  };
  ```

**산출물:**
- 상점 페이지
- 아이템 구매 시스템
- 인벤토리 관리

---

#### Day 14: UI 스타일링 및 Phase 1 완료
**작업 내용:**
- [ ] 전체 UI 일관성 확인
  - 색상 테마 통일
  - 버튼 스타일 통일
  - 간격, 패딩 일관성

- [ ] CSS 정리 및 최적화
  - 중복 제거
  - 변수 사용 (CSS Variables)
  ```css
  :root {
    --color-primary: #d4a574;
    --color-bg-dark: rgba(40, 35, 30, 0.9);
    --color-text-light: #ffffff;
    --color-border: #4a3a35;
  }
  ```

- [ ] 반응형 제거 (PC 전용 확정)
  - 최소 너비: 1024px 고정

- [ ] 성능 최적화
  - 불필요한 re-render 제거 (React.memo)
  - useCallback, useMemo 적용

- [ ] GitHub Pages 배포 준비
  ```bash
  npm run build
  ```
  - `vite.config.js`에 base 경로 설정
  ```javascript
  export default defineConfig({
    base: '/typing-rpg/',
    // ...
  });
  ```

**최종 테스트:**
- [ ] 모든 페이지 이동이 정상 작동하는가?
- [ ] 게임 플레이 → 결과 → 랭킹 저장이 정상인가?
- [ ] 상점에서 아이템 구매가 정상 작동하는가?
- [ ] LocalStorage에 데이터가 정상 저장되는가?
- [ ] 페이지 새로고침 후 데이터가 유지되는가?

**산출물:**
- 완성된 프론트엔드 MVP
- GitHub Pages 배포 가능한 빌드 파일

---

## 🔧 Phase 2: 백엔드 연동 (2주)

**목표:** Spring Boot API 구현 + SQLite 연동 + Render 배포

### Week 3: Spring Boot API 개발

#### Day 15-16: Spring Boot 프로젝트 세팅
**작업 내용:**
- [ ] Spring Initializr로 프로젝트 생성
  - Project: Gradle
  - Language: Java 17
  - Spring Boot: 3.x (최신 안정 버전)
  - Dependencies:
    - Spring Web
    - Spring Data JPA
    - SQLite JDBC Driver (별도 추가)
    - Lombok
    - Validation

- [ ] 프로젝트 구조 설정
  ```
  src/main/java/com/typing/rpg/
  ├── controller/
  ├── service/
  ├── repository/
  ├── entity/
  ├── dto/
  ├── config/
  └── TypingRpgApplication.java
  ```

- [ ] SQLite 설정 (`application.yml`)
  ```yaml
  spring:
    datasource:
      url: jdbc:sqlite:typingrpg.db
      driver-class-name: org.sqlite.JDBC
    jpa:
      database-platform: org.hibernate.community.dialect.SQLiteDialect
      hibernate:
        ddl-auto: update
      show-sql: true
  ```

- [ ] CORS 설정 (프론트엔드 연동)
  ```java
  @Configuration
  public class WebConfig implements WebMvcConfigurer {
      @Override
      public void addCorsMappings(CorsRegistry registry) {
          registry.addMapping("/api/**")
                  .allowedOrigins("http://localhost:5173", "https://<github-pages-url>")
                  .allowedMethods("GET", "POST", "PUT", "DELETE");
      }
  }
  ```

**산출물:**
- Spring Boot 프로젝트
- SQLite 데이터베이스 연결
- CORS 설정

---

#### Day 17-18: 엔티티 및 Repository 작성
**작업 내용:**
- [ ] Entity 작성

**Player.java**
```java
@Entity
@Table(name = "players")
@Data
@NoArgsConstructor
public class Player {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false)
    private String nickname;

    private Integer level = 1;
    private Integer exp = 0;
    private Integer gold = 0;
    private Integer hp = 100;
    private Integer maxHp = 100;
    private Integer atk = 10;

    @CreatedDate
    private LocalDateTime createdAt;
}
```

**GameScore.java**
```java
@Entity
@Table(name = "game_scores")
@Data
@NoArgsConstructor
public class GameScore {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "player_id")
    private Player player;

    private Integer contentId;
    private Integer score;
    private Double wpm;
    private Double accuracy;
    private Integer monstersDefeated;

    @CreatedDate
    private LocalDateTime playedAt;
}
```

**Item.java**
```java
@Entity
@Table(name = "items")
@Data
@NoArgsConstructor
public class Item {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String name;
    private String type; // weapon, armor
    private Integer price;
    private Integer atkBonus;
    private Integer hpBonus;
}
```

**PlayerItem.java** (구매한 아이템)
```java
@Entity
@Table(name = "player_items")
@Data
@NoArgsConstructor
public class PlayerItem {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "player_id")
    private Player player;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "item_id")
    private Item item;

    @CreatedDate
    private LocalDateTime purchasedAt;
}
```

- [ ] Repository 작성
```java
public interface PlayerRepository extends JpaRepository<Player, Long> {
    Optional<Player> findByNickname(String nickname);
}

public interface GameScoreRepository extends JpaRepository<GameScore, Long> {
    List<GameScore> findTop10ByOrderByScoreDesc();
    List<GameScore> findByPlayerOrderByPlayedAtDesc(Player player);
}

public interface ItemRepository extends JpaRepository<Item, Long> {
    List<Item> findByType(String type);
}

public interface PlayerItemRepository extends JpaRepository<PlayerItem, Long> {
    List<PlayerItem> findByPlayer(Player player);
}
```

**산출물:**
- JPA 엔티티 (Player, GameScore, Item, PlayerItem)
- Repository 인터페이스

---

#### Day 19-20: Service 레이어 작성
**작업 내용:**
- [ ] PlayerService.java
```java
@Service
@RequiredArgsConstructor
public class PlayerService {
    private final PlayerRepository playerRepository;

    public Player getOrCreatePlayer(String nickname) {
        return playerRepository.findByNickname(nickname)
                .orElseGet(() -> {
                    Player newPlayer = new Player();
                    newPlayer.setNickname(nickname);
                    return playerRepository.save(newPlayer);
                });
    }

    public Player updatePlayerStats(String nickname, Integer exp, Integer gold) {
        Player player = getOrCreatePlayer(nickname);
        player.setExp(player.getExp() + exp);
        player.setGold(player.getGold() + gold);

        // 레벨업 로직
        while (player.getExp() >= player.getLevel() * 100) {
            player.setExp(player.getExp() - player.getLevel() * 100);
            player.setLevel(player.getLevel() + 1);
            player.setMaxHp(player.getMaxHp() + 10);
            player.setHp(player.getMaxHp());
            player.setAtk(player.getAtk() + 2);
        }

        return playerRepository.save(player);
    }
}
```

- [ ] GameScoreService.java
```java
@Service
@RequiredArgsConstructor
public class GameScoreService {
    private final GameScoreRepository gameScoreRepository;
    private final PlayerService playerService;

    public GameScore saveScore(String nickname, GameScoreDto dto) {
        Player player = playerService.getOrCreatePlayer(nickname);

        GameScore score = new GameScore();
        score.setPlayer(player);
        score.setContentId(dto.getContentId());
        score.setScore(dto.getScore());
        score.setWpm(dto.getWpm());
        score.setAccuracy(dto.getAccuracy());
        score.setMonstersDefeated(dto.getMonstersDefeated());

        return gameScoreRepository.save(score);
    }

    public List<GameScore> getTop10Rankings() {
        return gameScoreRepository.findTop10ByOrderByScoreDesc();
    }
}
```

- [ ] ShopService.java
```java
@Service
@RequiredArgsConstructor
public class ShopService {
    private final ItemRepository itemRepository;
    private final PlayerItemRepository playerItemRepository;
    private final PlayerRepository playerRepository;

    public List<Item> getAllItems() {
        return itemRepository.findAll();
    }

    public PlayerItem purchaseItem(String nickname, Long itemId) {
        Player player = playerRepository.findByNickname(nickname)
                .orElseThrow(() -> new RuntimeException("Player not found"));
        Item item = itemRepository.findById(itemId)
                .orElseThrow(() -> new RuntimeException("Item not found"));

        if (player.getGold() < item.getPrice()) {
            throw new RuntimeException("Not enough gold");
        }

        player.setGold(player.getGold() - item.getPrice());
        player.setAtk(player.getAtk() + (item.getAtkBonus() != null ? item.getAtkBonus() : 0));
        player.setMaxHp(player.getMaxHp() + (item.getHpBonus() != null ? item.getHpBonus() : 0));
        playerRepository.save(player);

        PlayerItem playerItem = new PlayerItem();
        playerItem.setPlayer(player);
        playerItem.setItem(item);
        return playerItemRepository.save(playerItem);
    }
}
```

**산출물:**
- Service 레이어 (비즈니스 로직)
- 레벨업 시스템
- 상점 구매 로직

---

#### Day 21: Controller 및 DTO 작성
**작업 내용:**
- [ ] DTO 작성

**PlayerDto.java**
```java
@Data
public class PlayerDto {
    private Long id;
    private String nickname;
    private Integer level;
    private Integer exp;
    private Integer gold;
    private Integer hp;
    private Integer maxHp;
    private Integer atk;
}
```

**GameScoreDto.java**
```java
@Data
public class GameScoreDto {
    private Integer contentId;
    private Integer score;
    private Double wpm;
    private Double accuracy;
    private Integer monstersDefeated;
}
```

- [ ] Controller 작성

**PlayerController.java**
```java
@RestController
@RequestMapping("/api/players")
@RequiredArgsConstructor
public class PlayerController {
    private final PlayerService playerService;

    @GetMapping("/{nickname}")
    public ResponseEntity<Player> getPlayer(@PathVariable String nickname) {
        Player player = playerService.getOrCreatePlayer(nickname);
        return ResponseEntity.ok(player);
    }

    @PostMapping("/{nickname}/update-stats")
    public ResponseEntity<Player> updateStats(
            @PathVariable String nickname,
            @RequestParam Integer exp,
            @RequestParam Integer gold) {
        Player player = playerService.updatePlayerStats(nickname, exp, gold);
        return ResponseEntity.ok(player);
    }
}
```

**GameScoreController.java**
```java
@RestController
@RequestMapping("/api/scores")
@RequiredArgsConstructor
public class GameScoreController {
    private final GameScoreService gameScoreService;

    @PostMapping("/{nickname}")
    public ResponseEntity<GameScore> saveScore(
            @PathVariable String nickname,
            @RequestBody GameScoreDto dto) {
        GameScore score = gameScoreService.saveScore(nickname, dto);
        return ResponseEntity.ok(score);
    }

    @GetMapping("/rankings")
    public ResponseEntity<List<GameScore>> getRankings() {
        List<GameScore> rankings = gameScoreService.getTop10Rankings();
        return ResponseEntity.ok(rankings);
    }
}
```

**ShopController.java**
```java
@RestController
@RequestMapping("/api/shop")
@RequiredArgsConstructor
public class ShopController {
    private final ShopService shopService;

    @GetMapping("/items")
    public ResponseEntity<List<Item>> getAllItems() {
        return ResponseEntity.ok(shopService.getAllItems());
    }

    @PostMapping("/purchase/{nickname}/{itemId}")
    public ResponseEntity<PlayerItem> purchaseItem(
            @PathVariable String nickname,
            @PathVariable Long itemId) {
        PlayerItem playerItem = shopService.purchaseItem(nickname, itemId);
        return ResponseEntity.ok(playerItem);
    }
}
```

**산출물:**
- REST API 컨트롤러
- DTO 클래스

---

### Week 4: 프론트엔드 연동 및 배포

#### Day 22-23: 프론트엔드 API 연동
**작업 내용:**
- [ ] API 클라이언트 작성 (`src/api/client.js`)
```javascript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8080/api';

export const api = {
  // Player
  getPlayer: async (nickname) => {
    const res = await fetch(`${API_BASE_URL}/players/${nickname}`);
    return res.json();
  },

  updatePlayerStats: async (nickname, exp, gold) => {
    const res = await fetch(`${API_BASE_URL}/players/${nickname}/update-stats?exp=${exp}&gold=${gold}`, {
      method: 'POST'
    });
    return res.json();
  },

  // Score
  saveScore: async (nickname, scoreData) => {
    const res = await fetch(`${API_BASE_URL}/scores/${nickname}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(scoreData)
    });
    return res.json();
  },

  getRankings: async () => {
    const res = await fetch(`${API_BASE_URL}/scores/rankings`);
    return res.json();
  },

  // Shop
  getItems: async () => {
    const res = await fetch(`${API_BASE_URL}/shop/items`);
    return res.json();
  },

  purchaseItem: async (nickname, itemId) => {
    const res = await fetch(`${API_BASE_URL}/shop/purchase/${nickname}/${itemId}`, {
      method: 'POST'
    });
    return res.json();
  }
};
```

- [ ] LocalStorage → API 마이그레이션
  - 닉네임 입력 모달 추가
  - 게임 종료 시 API 호출
  - 랭킹 페이지 API 연동

- [ ] 닉네임 입력 컴포넌트 (`src/components/NicknameModal.jsx`)
```javascript
const NicknameModal = ({ onSubmit }) => {
  const [nickname, setNickname] = useState('');

  const handleSubmit = () => {
    if (nickname.trim()) {
      localStorage.setItem('typingRpg_nickname', nickname);
      onSubmit(nickname);
    }
  };

  return (
    <div className="modal-overlay">
      <div className="modal-content">
        <h2>닉네임을 입력하세요</h2>
        <input value={nickname} onChange={(e) => setNickname(e.target.value)} />
        <button onClick={handleSubmit}>시작하기</button>
      </div>
    </div>
  );
};
```

**산출물:**
- API 클라이언트
- 닉네임 시스템
- LocalStorage → API 전환

---

#### Day 24-25: 백엔드 배포 (Render.com)
**작업 내용:**
- [ ] Render.com 회원가입
- [ ] GitHub Repository 연결
- [ ] Web Service 생성
  - Build Command: `./gradlew clean build`
  - Start Command: `java -jar build/libs/typing-rpg-0.0.1-SNAPSHOT.jar`
  - Environment: Java 17

- [ ] SQLite 파일 영구 저장 설정
  - Render Disk 추가 (무료 1GB)
  - Mount Path: `/data`
  - application.yml 수정:
  ```yaml
  spring:
    datasource:
      url: jdbc:sqlite:/data/typingrpg.db
  ```

- [ ] 초기 데이터 추가 (items)
  - `data.sql` 작성
  ```sql
  INSERT INTO items (name, type, price, atk_bonus, hp_bonus) VALUES
  ('나무 검', 'weapon', 100, 5, 0),
  ('철 검', 'weapon', 300, 15, 0),
  ('가죽 갑옷', 'armor', 150, 0, 20),
  ('철 갑옷', 'armor', 400, 0, 50);
  ```

**산출물:**
- Render.com 배포된 백엔드
- 공개 API URL

---

#### Day 26-27: 프론트엔드 배포 및 통합 테스트
**작업 내용:**
- [ ] 환경 변수 설정
  ```
  # .env.production
  VITE_API_URL=https://your-backend.onrender.com/api
  ```

- [ ] GitHub Pages 배포
  ```bash
  npm run build
  # gh-pages 브랜치에 배포
  ```

- [ ] GitHub Actions 자동 배포 설정 (선택)
  ```yaml
  # .github/workflows/deploy.yml
  name: Deploy to GitHub Pages

  on:
    push:
      branches: [main]

  jobs:
    build-and-deploy:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: actions/setup-node@v3
        - run: npm install
        - run: npm run build
        - uses: peaceiris/actions-gh-pages@v3
          with:
            github_token: ${{ secrets.GITHUB_TOKEN }}
            publish_dir: ./dist
  ```

**통합 테스트:**
- [ ] 닉네임 입력 → 플레이어 생성 확인
- [ ] 게임 플레이 → 점수 저장 확인
- [ ] 랭킹 페이지에서 API 데이터 확인
- [ ] 상점 구매 → DB 저장 확인
- [ ] 새로고침 후 데이터 유지 확인

**산출물:**
- GitHub Pages 배포된 프론트엔드
- 완전히 작동하는 Full Stack 애플리케이션

---

#### Day 28: Phase 2 마무리 및 문서화
**작업 내용:**
- [ ] README.md 작성
  - 프로젝트 소개
  - 기술 스택
  - 주요 기능
  - 실행 방법 (로컬)
  - API 문서 링크
  - 배포 URL
  - 스크린샷

- [ ] API 문서 작성 (Swagger 또는 별도 문서)
- [ ] 코드 주석 정리
- [ ] 버그 수정 및 리팩토링

**산출물:**
- 완성된 README.md
- API 문서

---

## 🚀 Phase 3: 추가 기능 및 개선 (2주)

**목표:** 게임성 향상 + UI/UX 개선 + 무료 리소스 적용

### Week 5: 게임성 향상

#### Day 29-30: 다양한 게임 모드 추가
**작업 내용:**
- [ ] 시간 제한 모드
  - 60초 안에 최대한 많은 문장
  - 시간 초과 시 자동 종료

- [ ] 무한 모드
  - 플레이어 HP가 0이 될 때까지
  - 문장 무한 반복

- [ ] 난이도 시스템
  - Easy: 짧은 문장, 몬스터 약함
  - Normal: 중간 길이, 보통 몬스터
  - Hard: 긴 문장, 강한 몬스터

**산출물:**
- 3가지 게임 모드
- 난이도 선택 기능

---

#### Day 31-32: 콘텐츠 확장
**작업 내용:**
- [ ] 문장 세트 10개로 확장
  - 고블린 숲
  - 안개 초원
  - 어두운 동굴
  - 화산 지대
  - 얼음 성
  - 등등...

- [ ] 카테고리 추가
  - 판타지
  - 명언
  - 코딩 (프로그래밍 용어)
  - 일상 대화

- [ ] 다양한 몬스터
  - 각 콘텐츠별 고유 몬스터
  - HP, EXP, Gold 차별화

**산출물:**
- 10개 콘텐츠 세트
- 다양한 몬스터

---

#### Day 33-34: 업적 시스템
**작업 내용:**
- [ ] 업적 데이터 구조
  ```javascript
  const achievements = [
    { id: 1, title: "첫 걸음", desc: "첫 게임 완료", condition: "playCount >= 1" },
    { id: 2, title: "타자왕", desc: "WPM 100 달성", condition: "maxWpm >= 100" },
    { id: 3, title: "부자", desc: "골드 1000 획득", condition: "totalGold >= 1000" }
  ];
  ```

- [ ] 업적 달성 알림 (토스트)
- [ ] 업적 페이지

**산출물:**
- 업적 시스템
- 업적 페이지

---

#### Day 35: 사운드 효과 (선택)
**작업 내용:**
- [ ] 무료 사운드 리소스 찾기 (freesound.org)
  - 타이핑 사운드
  - 정답/오답 사운드
  - 레벨업 사운드
  - 구매 사운드

- [ ] 사운드 ON/OFF 설정

**산출물:**
- 기본 사운드 효과

---

### Week 6: UI/UX 개선 및 리소스 적용

#### Day 36-37: 무료 픽셀 아트 리소스 적용
**작업 내용:**
- [ ] 무료 리소스 사이트에서 다운로드
  - itch.io
  - OpenGameArt.org
  - Kenney.nl

- [ ] 필요한 리소스
  - 플레이어 스프라이트 (idle, attack)
  - 몬스터 스프라이트 (5-10종)
  - 아이템 아이콘 (무기, 방어구)
  - 배경 이미지 (던전, 숲 등)
  - UI 요소 (버튼, 패널)

- [ ] 이미지 최적화
  - WebP 변환
  - 크기 최적화

**산출물:**
- 픽셀 아트 리소스 적용

---

#### Day 38-39: 애니메이션 추가
**작업 내용:**
- [ ] 캐릭터 애니메이션
  - Idle (대기)
  - Attack (공격)
  - Hit (피격)

- [ ] 몬스터 애니메이션
  - Idle
  - Hit
  - Death

- [ ] UI 전환 애니메이션
  - 페이지 전환 fade
  - 버튼 hover 효과
  - 모달 등장 애니메이션

- [ ] 데미지 텍스트 애니메이션

**산출물:**
- 부드러운 애니메이션

---

#### Day 40-41: UX 개선
**작업 내용:**
- [ ] 로딩 상태 표시
  - API 호출 시 로딩 스피너
  - 이미지 로딩 placeholder

- [ ] 에러 처리
  - API 에러 시 사용자 친화적 메시지
  - 네트워크 오류 처리

- [ ] 튜토리얼 추가
  - 첫 실행 시 간단한 가이드
  - "다시 보지 않기" 옵션

- [ ] 키보드 단축키
  - ESC: 메뉴로 돌아가기
  - Enter: 게임 시작

**산출물:**
- 향상된 UX

---

#### Day 42: Phase 3 완료 및 최종 테스트
**작업 내용:**
- [ ] 전체 기능 테스트
- [ ] 성능 최적화
  - Lighthouse 점수 확인
  - 이미지 최적화
  - 코드 스플리팅

- [ ] 크로스 브라우저 테스트
  - Chrome
  - Firefox
  - Edge

- [ ] 버그 수정
- [ ] 최종 배포

**산출물:**
- 완성된 TypingRPG v1.0

---

## 📂 최종 디렉토리 구조

### 프론트엔드
```
typing-rpg-frontend/
├── public/
│   ├── assets/
│   │   ├── characters/
│   │   ├── monsters/
│   │   ├── items/
│   │   └── backgrounds/
│   └── sounds/
├── src/
│   ├── api/
│   │   └── client.js
│   ├── components/
│   │   ├── Navbar.jsx
│   │   ├── GameHUD.jsx
│   │   ├── TypingInput.jsx
│   │   ├── HealthBar.jsx
│   │   ├── GameResult.jsx
│   │   ├── NicknameModal.jsx
│   │   └── ...
│   ├── pages/
│   │   ├── Home.jsx
│   │   ├── ContentSelect.jsx
│   │   ├── GamePlay.jsx
│   │   ├── Ranking.jsx
│   │   ├── Shop.jsx
│   │   └── Profile.jsx
│   ├── context/
│   │   └── GameContext.jsx
│   ├── hooks/
│   │   ├── useTyping.js
│   │   └── usePlayer.js
│   ├── utils/
│   │   ├── typingEngine.js
│   │   └── storage.js
│   ├── data/
│   │   ├── sentences.js
│   │   └── achievements.js
│   ├── styles/
│   │   ├── global.css
│   │   └── components/
│   ├── App.jsx
│   └── main.jsx
├── .env.production
├── vite.config.js
└── package.json
```

### 백엔드
```
typing-rpg-backend/
├── src/main/java/com/typing/rpg/
│   ├── controller/
│   │   ├── PlayerController.java
│   │   ├── GameScoreController.java
│   │   └── ShopController.java
│   ├── service/
│   │   ├── PlayerService.java
│   │   ├── GameScoreService.java
│   │   └── ShopService.java
│   ├── repository/
│   │   ├── PlayerRepository.java
│   │   ├── GameScoreRepository.java
│   │   ├── ItemRepository.java
│   │   └── PlayerItemRepository.java
│   ├── entity/
│   │   ├── Player.java
│   │   ├── GameScore.java
│   │   ├── Item.java
│   │   └── PlayerItem.java
│   ├── dto/
│   │   ├── PlayerDto.java
│   │   └── GameScoreDto.java
│   ├── config/
│   │   └── WebConfig.java
│   └── TypingRpgApplication.java
├── src/main/resources/
│   ├── application.yml
│   └── data.sql
├── build.gradle
└── settings.gradle
```

---

## 📊 API 설계 (RESTful)

### Player API
```
GET    /api/players/{nickname}              - 플레이어 정보 조회/생성
POST   /api/players/{nickname}/update-stats - 스탯 업데이트 (exp, gold)
```

### Score API
```
POST   /api/scores/{nickname}               - 점수 저장
GET    /api/scores/rankings                 - 전체 랭킹 조회 (Top 10)
GET    /api/scores/player/{nickname}        - 개인 점수 기록
```

### Shop API
```
GET    /api/shop/items                      - 아이템 목록
POST   /api/shop/purchase/{nickname}/{itemId} - 아이템 구매
GET    /api/shop/inventory/{nickname}       - 보유 아이템 목록
```

---

## 🗄 데이터베이스 스키마

### players
```sql
CREATE TABLE players (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nickname VARCHAR(50) UNIQUE NOT NULL,
    level INTEGER DEFAULT 1,
    exp INTEGER DEFAULT 0,
    gold INTEGER DEFAULT 0,
    hp INTEGER DEFAULT 100,
    max_hp INTEGER DEFAULT 100,
    atk INTEGER DEFAULT 10,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### game_scores
```sql
CREATE TABLE game_scores (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    player_id INTEGER NOT NULL,
    content_id INTEGER NOT NULL,
    score INTEGER NOT NULL,
    wpm REAL NOT NULL,
    accuracy REAL NOT NULL,
    monsters_defeated INTEGER DEFAULT 0,
    played_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (player_id) REFERENCES players(id)
);
```

### items
```sql
CREATE TABLE items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(50) NOT NULL,
    type VARCHAR(20) NOT NULL,
    price INTEGER NOT NULL,
    atk_bonus INTEGER DEFAULT 0,
    hp_bonus INTEGER DEFAULT 0
);
```

### player_items
```sql
CREATE TABLE player_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    player_id INTEGER NOT NULL,
    item_id INTEGER NOT NULL,
    purchased_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (player_id) REFERENCES players(id),
    FOREIGN KEY (item_id) REFERENCES items(id)
);
```

---

## ✅ 완료 기준 (Definition of Done)

### Phase 1
- [ ] React 프로젝트 실행 가능
- [ ] 타이핑 게임 플레이 가능
- [ ] WPM, 정확도 계산 정상 작동
- [ ] LocalStorage 저장/로드 정상
- [ ] 모든 페이지 접근 가능
- [ ] GitHub Pages 배포 완료

### Phase 2
- [ ] Spring Boot API 실행 가능
- [ ] SQLite 데이터 저장 정상
- [ ] 프론트-백엔드 통신 정상
- [ ] 랭킹 시스템 작동
- [ ] Render.com 배포 완료

### Phase 3
- [ ] 픽셀 아트 적용 완료
- [ ] 애니메이션 적용
- [ ] 10개 콘텐츠 세트 완성
- [ ] 업적 시스템 작동
- [ ] 최종 테스트 통과

---

## 🎯 다음 단계 (Phase 4+)

Phase 3 완료 후 선택적으로 추가할 기능:

### 인증 시스템 (선택)
- 닉네임 + 비밀번호
- JWT 토큰
- 세션 관리

### 관리자 페이지 (선택)
- 콘텐츠 CRUD
- 아이템 관리
- 유저 관리

### 소셜 기능 (선택)
- 친구 추가
- 챌린지 (경쟁)
- 채팅

### 고급 기능 (선택)
- 멀티플레이어 (WebSocket)
- 실시간 랭킹
- 일일 미션

---

## 📝 주의사항

### 개발 시 유의할 점
1. **커밋 자주 하기:** 기능 단위로 작은 커밋
2. **테스트 먼저:** 기능 구현 후 바로 테스트
3. **문서화:** 복잡한 로직은 주석 작성
4. **성능 고려:** 불필요한 렌더링 방지
5. **보안:** SQL Injection 방지 (JPA 사용 시 자동)

### Render.com 무료 티어 제약
- **콜드 스타트:** 15분 미사용 시 서버 sleep → 첫 요청 느림 (30초)
- **월 750시간:** 한 달 무료 (충분함)
- **해결책:** 헬스체크 핑 (5분마다) - 선택 사항

### GitHub Pages 제약
- **정적 파일만:** HTML, CSS, JS만 호스팅
- **API 불가:** 백엔드는 별도 배포 필요

---

## 📅 예상 일정 요약

| Phase | 기간 | 주요 산출물 |
|-------|------|-----------|
| Phase 1 | 2주 | 프론트엔드 MVP, LocalStorage 버전 |
| Phase 2 | 2주 | Spring Boot API, DB 연동, 배포 |
| Phase 3 | 2주 | 리소스 적용, 추가 기능, 완성 |
| **합계** | **6주** | **완성된 TypingRPG 포트폴리오** |

---

## 🚀 시작하기

### 즉시 시작할 수 있는 첫 작업

```bash
# 1. 프로젝트 생성
npm create vite@latest typing-rpg-frontend -- --template react
cd typing-rpg-frontend
npm install

# 2. Git 초기화
git init
git add .
git commit -m "Initial commit: React + Vite setup"

# 3. GitHub에 푸시
# (GitHub에서 Repository 먼저 생성)
git remote add origin https://github.com/<username>/typing-rpg.git
git push -u origin main

# 4. 개발 서버 실행
npm run dev
```

---

## ❓ 추가 질문

계획을 검토하시고 다음 사항을 알려주세요:

1. **시작 시점:** 언제부터 개발을 시작하시겠습니까?
2. **개발 시간:** 하루에 얼마나 작업 가능하신가요? (일정 조정 가능)
3. **우선순위 변경:** 위 계획에서 조정하고 싶은 부분이 있나요?
4. **추가 기능:** 꼭 포함하고 싶은 기능이 더 있나요?

**답변해주시면 바로 개발을 시작할 수 있도록 첫 번째 작업을 안내해드리겠습니다!** 🎮
